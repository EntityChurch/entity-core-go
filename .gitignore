# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Built CLI binaries (from accidental `go build` without -o).
# One entry per cmd/ dir — ADD A LINE WHEN YOU ADD A cmd/. This list drifted
# behind cmd/ twice; a missed entry shows up as a multi-MB untracked binary at
# the repo root (caught 2026-08-01 with /signaling-punch).
/bench-localfiles
/compare-types
/compute-corpus
/dump-messages
/dump-types
/encryption-vectors
/entity-peer
/entity-shell
/entity-sync
/fetch-published-fixture
/peer-manager
/peerissued-fixtures
/probe-files
/probe-grant
/probe-hello
/probe-ingest
/probe-peer
/probe-signaling
/publish-fixture
/registry-issue-binding
/registry-request-binding
/relay-fixtures
/scan-probe
/signaling-meet
/signaling-punch
/v767-corpus-verify
/v767-phase2-pins
/validate-peer
/webrtc-vectors
/wire-conformance
/wire-conformance-consume

# Test binary
*.test

# Output of go coverage
*.out
*.prof

# Wire-trace / debug logs (never commit — large, ephemeral, may leak peer addrs)
*.log

# Go workspace (go.work is tracked — it defines our multi-module layout)
go.work.sum

# Per-machine podman resource-cap overrides (see Makefile + RESOURCE-CAPS.md)
caps.local.mk

# Dependency directory
vendor/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
