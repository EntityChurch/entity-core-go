
# entity-core-go

Read **AGENTS-STANDARD.md** first (ecosystem conventions). This file adds entity-core-go specifics.

## Overview

Go implementation of the Entity Core Protocol v7 — the third ground-up reference implementation (after Python and Rust), and the SDK prototype: **Go leads on new features**, cross-impl convergence is downstream feedback, not a gate.

## How we work here — tier **CORE**

This repo runs the entity-OS methodology at the **Core** tier — the framework is
`METHODOLOGY.md` (injected, identical everywhere; read it once). Conformance gates the wire
here. It does **not** catch process drift, stale build-state claims, unaccounted accumulation,
or a discipline quietly eaten by a competing legitimate pressure. Those need the ratchet.

**This repo diagnosed the reason the tier exists.** `docs/reviews/2026-08-12-discipline-audit-are-we-still-following-our-own-rules.md`
found the rules living in a sibling's guide, an ADR in a third repo, and this file — and named
the consequence: *"An audit should not have to assemble its own standard first. That is
plausibly the root cause of drift being invisible."* It also found the mechanism, which is the
part worth carrying: *"A legitimate pressure ate a discipline, and nothing in the process
noticed."* Reducing cross-repo round trips is a real constraint; it cannibalised *spec
arbitrates ambiguity, do not vote* because both point at the same act.

What binds today:

- **Universal disciplines D1–D12** (`METHODOLOGY.md` §4) — apply as written; nothing to re-derive.
- **The review questions** (§6) — run on every diff.
- **The Audit Doctrine A0–A12** (§7.2) — open it for *"Y is broken"* or *"something feels
  wrong,"* including when the thing that feels wrong is our own process. **A1 is the prime:
  trace a value before you theorize.**
- **The ratchet** — every audit ends by syncing what it taught into this file, same session.
  **If it didn't land here, it didn't land.**
- **The promotion ladder** (§3) — bit us once → an anti-pattern entry; a second time in a
  different shape → a ratified discipline. Candidates are applied, not yet claimed to generalize.
- **The divergence rule, as a standing discipline.** When a cross-impl divergence is measured,
  the response is chosen from `GUIDE-CONFORMANCE` §4's table **before** a fix is written, and
  the row is named in the commit. **A fix whose justification includes a count of
  implementations is the smell.** All-three-differ = spec ambiguity, tighten the spec; two-and-two
  = spec arbitrates, do not vote.
  > **The smell rides a ROUTED recommendation too, not just a fix — a lone seat matching a spec
  > MUST outranks a cohort emitting a convenience (earned 2026-09-04, B2; arch ruled against go).**
  > go correctly flagged B2 (`unsupported_mode`@501) as a genuine spec-vs-spec ruling and did not
  > converge it — but the *recommendation* attached to the route weighted the 2-1 cohort ("go's
  > §9.1 reading, matching rust"). arch ruled py's way: the corpus MUST (`EXTENSION-REGISTRY`
  > §6a.9.2 deliberately pins `unsupported_mode`) governs; a census of what peers emit cannot tell
  > you what the corpus mandates (arch AP-22). **Enforcement: before recommending a reading on a
  > routed spec-vs-spec, grep the corpus for the spelling the spec MANDATES (`[MUST]`, "pinned",
  > "MUST NOT fall back") — and if a lone seat holds that spelling against the majority, its
  > position is the default recommendation, not the cohort's.** No new discipline (the divergence
  > rule reaches the recommendation, not only the fix); recorded because the routing was right and
  > the recommendation was still wrong, which the "do not vote" framing did not obviously cover.
  > Sibling win the same cycle, worth the pair: arch's *prose* worklist ("go's 4 local-files
  > `storage_error` sites → `io_error`") contradicted arch's own just-landed line 836 (a tree bind
  > is `storage_error` anywhere); the 4 sites are `hctx.TreeSet` binds, so go verified against the
  > spec TEXT, kept `storage_error`, and routed the discrepancy rather than implementing the prose.
  > **When a routing packet's prose and the landed spec text diverge, implement the text and route
  > the prose** — the same "a newly-landed clause contradicting a prior ruling is a spec-issue to
  > route" discipline, now covering an arch *claim about our tree* that the spec text refutes.

**The close-out ratchet does not owe a NEW rule every time — reaching for one is itself the
anti-pattern** (earned 2026-08-16 by an audit of this session's own ratchets). The E-1 close-out
minted a "path helper must reference the owner's exported path function" invariant from
`encTierCCertPath` restating a literal. Traced (A1): the literal was byte-identical to
`identity.PublicCertPath`, it never diverged, no peer corrected it, and it only re-stated §8.4.2 /
D12 one layer down — **no bite, so below even the anti-pattern floor** ("bit us once"). It was sought
out to satisfy the ritual that a feature ends with a ratchet, and it diluted the load-bearing
protocol-invariants list; removed. The rule this earns: **a close-out whose feature only re-applied
existing disciplines closes out by recording that ("no new discipline earned"), not by minting a
novel one.** Enforcement: every ratchet addition names its bite — the incident, the peer correction,
or the `(file, commit)` where it manifested; an addition that can only cite "noticed while
implementing" is a candidate at most, and usually already covered. Audit ratchet additions against
the promotion ladder, not against the calendar.

**Owed** (from the 2026-08-12 audit, promoted here so it stops being invisible): a standing
`DISCIPLINE-*` doc assembling this repo's own rules with an anti-pattern catalog; G-7, the
re-verification of the 20 build-state assertions in `cmd/peer-manager` flag help pinned to 18
stale sibling commits; and the five domains its §5 named as unaudited (§2.4a negative halves ·
§5.2/§5.2a label correctness · the ~25 dated ROUTING/HANDOFF docs for the same stale-claim
defect class applied to prose · whether the volume of cross-repo routing is itself a process
defect).

## Setup / environment

- **Go**, `go.work` multi-module workspace — three modules, all under `go.entitychurch.org/entity-core-go/`: **`core`** (protocol library), **`ext`** (system extensions), **`cmd`** (CLIs / validation / interop). Import paths always carry the module prefix, e.g. `go.entitychurch.org/entity-core-go/core/hash`.
- **External deps (two):** `github.com/fxamacker/cbor/v2` (CBOR / ECF — `CoreDetEncOptions()`), `github.com/mr-tron/base58` (PeerID encoding). Dep gate: stable + predictable + 30+ days old, and prefer the library that drops in **without an adapter** — a "small adapter layer" is a sign of shape mismatch. When a handoff already names a lean pick that clears the bar, take it; don't re-litigate an obvious call.
- **Spec is upstream, and the V8 split moved it — there are now TWO spec repos, both siblings:**
  - **`../entity-core-protocol/specs/`** — the core protocol floor: `ENTITY-CORE-PROTOCOL.md`, `ENTITY-CBOR-ENCODING.md`, `ENTITY-NATIVE-TYPE-SYSTEM.md`. No `EXTENSION-*` lives here.
  - **`../entity-system-architecture/`** — everything else: `specs/extensions/EXTENSION-*.md` (all 25, incl. `EXTENSION-CONTINUATION.md`, `EXTENSION-NETWORK.md`), plus **`docs/proposals/PROPOSAL-*.md`** (where handed proposals actually land), `docs/status/` (arch handoffs, `WORKSTREAMS.md`), `docs/research/`.

  **`entity-core-architecture` is the pre-split repo — STALE. Do not read specs or proposals from it.** It still exists on disk and still contains an old `v7.0-core-revision/` tree, so a search there returns plausible-looking but outdated hits, and a search for a *new* proposal returns nothing at all. That cost a full cycle on 2026-07-19: a rebuilt `PROPOSAL-CONTINUATION-BOUNDS-PROPAGATION` was reported "not on disk" because only the dead repo was searched. **If a handed doc seems missing, you are in the wrong repo — check `../entity-system-architecture/docs/proposals/` before concluding anything is absent.** **A second manifestation, 2026-08-20: a CODE PATH pointing at the dead repo doesn't error — it silently degrades.** `cmd/internal/validate/profile_v9_drift_test.go`'s core-profile drift gate `Skipf`s when its spec path is missing, and its path pointed at `../entity-core-architecture/...ENTITY-CORE-PROTOCOL-V7.md` (never present since the split), so **the guard silently skipped for its entire existence — a drift gate that never once fired.** Repointed to `../entity-core-protocol/specs/ENTITY-CORE-PROTOCOL.md` (where §9.0/§9.5 live) at `<this commit>` and it now PASSes (fires). The rule this earns: **a Skip-on-missing-file guard is only a guard if its path is live — grep the tree for `entity-core-architecture` in any non-comment path (test fixtures, tool inputs, spec-derivation guards), because a dead path turns a green gate vacuous and no failing run ever reveals it.** (Same STALE-repo trap, new surface: a search miss vs a silent guard.)
- Other impls: `../entity-core-py/`, `../entity-core-rust/` (note: **`entity-core-rust`, never `entity-core-rs`**). All siblings sit beside this repo in the same parent dir — read from disk, don't interrogate.

## Build & test

```bash
go test ./core/... ./ext/... ./cmd/...        # full suite (all three modules)
go vet  ./core/... ./ext/... ./cmd/...         # vet all packages
go test -race ./core/... ./ext/... ./cmd/...   # race detector
go test -run TestName ./core/ecf/...           # single / targeted (stdlib testing, table-driven, no assertion lib)
go build ./...                                  # verify compilation
```

**Peers & validation — use the purpose-built tooling, the documented way.** No `go install`, no custom env, no one-off compiles, no backgrounding `entity-peer` with `&`, no building to `/tmp`. If a peer seems stale, stop and restart it — `peer-manager` rebuilds fresh on start.

> **You own your peer lifecycle. NEVER ask the operator to kill a process (earned 2026-09-09 — the operator's standing instruction, after repeated `pkill` asks).** Every peer you start goes through `peer-manager` (which tracks it), and you end a session by tearing your own down: `go run ./cmd/peer-manager stop --all`. The golden rule is *you do not kill processes* — that means you do not `pkill`/`kill` AND you do not push the kill onto the operator; the two are the same failure wearing different clothes. **A stray raw peer from another session is a PORT COLLISION to route around, not a kill to request:** it is not `peer-manager`-managed (so `stop --all` won't touch it, and that is correct — it is not yours), so run on free ports (`POLL_PORT=<free> PI_PORT=<free>`, `validate-complete.sh` already pre-flights both and aborts exit 2 with the free-port re-run line). Asking the operator to `pkill` a container/peer so your ports are clear is banned; pick free ports instead. If a genuinely un-routable-around collision exists, say so once and move on — do not block the work on it and do not hand him a kill command.

```bash
go run ./cmd/peer-manager start --name p1 --type go|python|rust --debug   # ALWAYS --debug
go run ./cmd/peer-manager list | stop --all                              # (Go = host build; Rust/Python via podman)
go run ./cmd/validate-peer -addr host:port [-identity NAME] [-category NAME] [-failures-only] [-exclude c1,c2] [-json] [-verbose]
go run ./cmd/validate-peer -peers h1:p,h2:p -identity NAME                # multi-peer convergence
go run ./cmd/validate-peer -list-categories                              # live ~60-category set (from validate.AllCategories())
./scripts/validate-peers.sh [python]                                     # scripted (SAVE=1 to save JSON)
TYPES=go,rust,python ./scripts/test-cross-peer.sh                        # convergence test
```

- `--profile core` scores the **16** core-profile categories (V7 §9.0); `--profile full` (default) runs all. It read **14** until 2026-08-12 — stale since v7.75 folded `concurrency` and `resource_bounds` into §9.0. Don't hand-count it: `coreProfileCategories` (`cmd/internal/validate/profile.go`) is the set, and `TestCoreProfileCategoriesMatchSpec` re-derives it from the spec text.
- **A new MUST-REFUSE on a data shape ripples to every PRODUCER of that shape — the oracle and the harness included, and `go test` alone will not catch it (earned 2026-08-18, CAP D11/D12).** Landing "reject a policy with null default_ttl" broke, silently, three producers of the now-invalid shape that the unit suite is blind to: the `registry_issuer` conformance checks that seed `{Mode: Open}` with no ttl (they'd have gone RED against an armed peer), `set_issuer_policy_replaces_whole` (it *asserted* the field could be cleared — now forbidden), and `validate-complete.sh`'s `--issuer-policy-mode open` arming with no `--issuer-policy-default-ttl` (the armed registry would fail its own register vectors at D12). This is the sibling of "enumerate every surface a rule names" on a different axis: a refusal rule's blast radius is every place that MINTS or SEEDS the refused shape — impl mint paths, test fixtures, **the oracle's own positive-path checks, and the peer-arming flags** — plus a matching negative conformance check. Enforcement: when you add a refuse-this-shape rule, `git grep` every constructor of the shape (here `IssuerPolicyData{`, the arming flags, `setIssuerPolicy`), and run the live category against an armed peer — a green `go test` is not evidence the wire suite still passes.
  > **Same-session addendum, but it bit again (2026-08-18 (c), earned — py caught it, not us).** The rule above failed twice over in one commit-family: (1) its producer list — "impl mint paths, test fixtures, the oracle's positive-path checks, the peer-arming flags" — **omitted the conformance FIXTURE GENERATOR** (`cmd/peerissued-fixtures`), a `main` package with a `-wire` self-verify that is invisible to BOTH `go test` and a raw `validate-peer` run; d948b4f's F2/D3 (peer-issued binding MUST carry a non-null ttl) made the generator refuse its own RESOLVE-1/PRECEDE-1 output, so `validate-complete.sh` aborted at step one for **every seat** and no impl could produce a conformance number. (2) The sweep was scoped to the *same-session* shape (D11/D12 policy) and never re-run against the **prior-session** refuse-rule in the same family (d948b4f F2/D3 binding), which had the identical blast radius unswept. The completed rule: a refuse-this-shape sweep's producer set includes **every generator that MINTS the shape for conformance, the fixture generators included** (`git grep` the shape's constructor across `cmd/*fixtures*` too, not just impl + harness), and landing a *new* refuse-rule obliges re-sweeping **every prior refuse-rule on a related shape** — a refusal family's producers accumulate across sessions. **Enforcement, hard: a close-out that claims "green" MUST have run `scripts/validate-complete.sh` to exit 0 and say so — `go test` green is not the release gate, and a close-out that names "full suite green" without naming the suite is how two gate failures (this fixture blocker AND pass-0b register drift) shipped under one.** (This is ADR-0012 green≠release, with the specific bite that the gate lives in a `main` self-verify and a static register pass, neither of which the unit runner touches.)
  > **Sharpened 2026-08-22 — read the gate's exit FROM the gate, never through a pipe: `validate-complete.sh | tail` returns TAIL's 0 and masks the gate's own exit 1 (earned; a prior close-out shipped on the masked form).** The rule above says a close-out claiming "green" MUST run `validate-complete.sh` to exit 0 — but running it as `./scripts/validate-complete.sh 2>&1 | tail` reports `[exited with code 0]` from **tail**, not the script, because a bash pipeline without `pipefail` takes the last command's status. Line 532 of the script exits 0 only if all six `RC*` are 0; the human-readable tell is its own summary line — `PASS 0 exit 0 · … · PASS 1 exit 1 · …` — where **any nonzero `PASS k exit` means the gate FAILED**, whatever a piped `[exited with code 0]` says. This bit twice: this session's first run printed `[exited with code 0]` over a real exit 1, and the immediately-prior go close-out claimed *"exit 0 on all six passes"* while the transcript's own summary read `PASS 1 exit 1` — the masked exit shipped as a green sign-off. **Enforcement: run the gate UNPIPED to a file and read `$?` (`./scripts/validate-complete.sh >log 2>&1; echo $?`), or `set -o pipefail`; and a close-out that quotes "exit 0" must quote the six per-pass `exit N` values, not a pipeline's status.** Same family as "presence is not validity" (a green `plan` that made no call) — here a green *exit* that was never the gate's. The nonzero this session was itself instructive but NOT a code defect: the six `peer_issued` FAILs were a sibling's leftover peer squatting `PI_PORT` 9401 (core-py's documented hazard — re-run with `PI_PORT=<free>`), a cross-session environmental collision, not a go regression; re-run clean on 9411. **No new discipline in the C-1..C-4 corpus work itself** — the de-version fold RE-APPLIED the standing "a Skip-on-missing-file guard is only a guard if its path is live" rule (the PASS 0 `[ -d …/v767 ]` guard and `corpus.Check()`'s `ErrAbsent` both went quiet, exactly as predicted, when arch deleted `v767/`), recorded per the close-out floor; the only rule earned is the pipe-masks-exit one above.
  > **Third manifestation 2026-09-06 — pass 0b flips from a SIBLING'S spec-move alone, so a green go-code diff is NOT evidence the gate still exits 0 (py caught it; no new discipline, the enforcement is the same "run the gate unpiped to a true exit 0").** The cycle-5 close-out (`2026-09-05-b`) claimed "exit 0 all six passes" and had NOT been run to a true exit 0: pass 0b (the static conformance register, no peer) was exiting 1 on `registry.v16_ttl_ceiling_reread [was PARTIAL]` — a baseline entry that stopped applying because arch's REGISTRY v1.17/v1.18 citations began resolving, flipping the check PARTIAL → RESOLVED **with zero change to go's tree**. The baseline was last written cycle-2 `be35f90`; four go commits landed after it touching only tree/content, all unit-green, all vet-clean — and pass 0b was red the whole time, invisible to every go-scoped signal because the input that moved was a *spec repo*. The tell: pass 0b reads the register against a committed baseline, and the register resolves citations against `../entity-core-protocol` and `../entity-system-architecture` — so **any arch spec bump can flip a citation's resolution and drift the baseline without a single go byte changing.** Re-pinned (`go run ./cmd/conformance-register -write-baseline`, one line removed — a gain, not a regression), pass 0b → exit 0, full gate re-run unpiped on free ports to a true `PASS 0..4 exit 0`. **Enforcement (unchanged, just re-stated with the new trigger): after any arch spec move — and ALWAYS before a close-out — run `go run ./cmd/conformance-register -check` (or the whole gate unpiped) and read its own exit; a green `go test`/`go vet`/unit suite is silent on a pass-0b baseline drift, because the drift's cause lives in a sibling repo.**
  > **Sharpened again 2026-09-04 — the http-poll port collision is WORSE than the PI_PORT one, because its FALSE FAILs are indistinguishable from a real conformance defect, and the peer FAIL-OPENs on its own bind failure (candidate — the pre-flight guard is the enforcement point; cost a full bisect to attribute).** The 9401 hazard above produced `peer_issued` FAILs recognizable as a posture artifact. A leftover peer squatting `POLL_PORT` (9451, the `--http-poll-addr` listener) is nastier: `entity-peer` logs `WARNING: http-poll listener: … address already in use` and CONTINUES with **no content server**, so `serving_mode` 404s EVERY in-scope serve — ~23 checks — and the gate scores them as conformance FAILs that look exactly like a real serving defect. This session's go gate read `PASS 1 exit 1` with 23 `serving_mode` FAILs on a clean tree; only a `git stash` bisect (clean `c14a1d3` reproduced identically → not my diff) plus tracing the peer log to the bind WARNING (A1, trace the value — the tell was `content_get_in_scope_status 404, want 200` while `pass 2`'s namespace-scoped serving PASSed) attributed it. **The peer's warn-and-continue is a fail-open on its own configuration (the "a control MUST fail closed on its own config" discipline, applied to the serving listener surface); the gate must fail closed for it.** Enforcement, landed: `validate-complete.sh` now PRE-flights `POLL_PORT` and `POLL_PORT+1` (a bare `/dev/tcp` connect BEFORE the peer starts — a squatted port IS listening, so an *after*-start liveness probe cannot tell the target's own listener from the squatter's) and aborts exit 2 with the free-port re-run command rather than emitting false FAILs. Two sub-lessons: (1) run the gate on free ports on any box that has run peers this session (`POLL_PORT=<free> PI_PORT=<free>`), and never label a `serving_mode` FAIL a defect without checking the target log for the bind WARNING; (2) a bash gotcha caught building the guard — `exec 3>&- 2>/dev/null` redirects the SHELL's stderr to `/dev/null` for good (an `exec` with a redirection applies it to the current shell), which silently swallowed the abort message; close an fd with a bare `exec 3>&-`. **Re-application of "fail closed on own config," not a novel discipline** — recorded because the enforcement (pre-flight port guard) is concrete and the false-FAIL-vs-real-defect indistinguishability is the new, sharper bite.
  > **Completion on the CHECK axis (2026-08-19, §4.3 set-resolver-config close-out — no new discipline, recorded per the floor).** The producer-sweep rule above is written for a refuse-*shape*; the same blast-radius holds one axis over, for landing a new *validate-peer check*. Adding `v15_dispatch_config_refused` / `v16_ttl_ceiling_reread` was `go test`-green and `validate-peer -category registry`-green, and STILL failed the gate at **pass 0b** — the static conformance-register (`cmd/conformance-register/baseline.json`) flags any check whose citation is newly unresolved, and a new check is by definition newly-present. So a new check's producer surfaces are: the **type-count guards** (`core/types/types_test.go`) when it adds a type, and the **conformance-register baseline** (re-pin with `go run ./cmd/conformance-register -write-baseline` once the citation is confirmed grounded) — both static gates a green `go test` and even a green single-category `validate-peer` run do not touch. This is the same "name the suite, run `validate-complete.sh` to exit 0" enforcement, with the bite that the two gates a new check trips are **passes 0 and 0b**, upstream of any peer.
- **Tightening a shared MATCHER/predicate ripples to every GRANT authored against its loose semantics — the same blast-radius discipline, one axis over from data-shape refusals (earned 2026-08-18 (o), the §5.4 self-match).** Removing `MatchesPattern`'s bare-prefix self-match (`X/*` no longer matches bare `X`, restoring canonical §5.4) was a *correctness* fix on one function, but authorization is CHECKED through that function, so it silently invalidated every capability grant whose scope relied on the loose match. It 403'd `maintain-peer`/`status`/`reconnect` because `ext/network`'s handler grant listed `system/network/*` as its resource and depended on that covering the BARE `system/network` the ops actually target — seven tests red in a package the matcher fix never mentions. The producer-set of a data-shape refusal is "every place that MINTS the shape" (the rule above); the producer-set of a matcher-tightening is **every grant, default-connection-grant, and scope config whose include/exclude patterns were written against the OLD behavior**. Enforcement: when you tighten a shared matcher/canonicalizer/predicate, `git grep` the grant constructors (`CapabilityScope{`, `Resources:`, `Handlers:`, default-grant builders) for patterns that lean on the loosened case, and run the FULL suite — the authorization tests that move live nowhere near the matcher's own package. The tell you got it right: rust/py already carried the tight matcher, so their grants already list the bare path explicitly — a cross-impl divergence in the *permissive* direction is a grant you are about to have to make explicit. (max_ttl this same session was the sibling data-shape case and re-applied the existing D11/D12 sweep verbatim — no new discipline there; and the published-root peer-id-segment fix re-applied the double-qualify D12 + "shared writer/reader helper passes Go-on-Go deceptively, the cross-impl FAIL is the true signal" disciplines — **no new discipline earned on either**, recorded per the close-out floor.)
  > **Inverse axis, 2026-08-23 (candidate — rust caught it in their tree, and go had the identical defect): making a check READ a field it never read before turns every existing SPELLING of that field from documentation into an enforcement input, and a spelling that is harmless as prose can be wrong as a ceiling.** (o) above is "the matcher changed, so grants written against the old semantics broke." This is the other direction: the matcher did not move — `49d4a03` (§5.2 Dimension 3 binds in-process sub-dispatch) simply added `Resource:` to the `ExecuteData` literal `makeLocalExecute` hands `CheckPermission`, and **`CheckResourceScope` began reading the `Resources` field of the §6.9 default per-handler self-grant, which until that commit nothing had ever read.** §6.9 describes that default as "all resources" and `createHandlerGrants` encoded it as bare `"*"` — but §5.5/PR-8 `capability.Canonicalize` resolves a bare `*` in a cap RESOURCE to `/{granter}/*`, i.e. **own namespace only**, so a default-scope handler could no longer write the foreign-namespace subtrees its own store legitimately holds under V7 §1.4 (Category A — a `follow` mirror at `/{them}/app/...`, a cached foreign content site). Measured, not reasoned: ceiling `["*"]` → `/2K8tBrDD.../*`, target `/2KJSNFpW.../app/test/...`, `IsCoveredBy` false, `403 capability_denied` on `system/tree:put`, while the identical binding through the bootstrap `LocationIndex` path succeeded — **the control is what makes the 403 attributable to the authorization encoding rather than to the store** (carry-the-teeth, ADR-0012). Two enforcement points. **(1) When you make a check consult a field it did not consult before, `git grep` every PRODUCER of that field and re-read each spelling as an enforcement input, not as a description** — the type change forced no call site here, so nothing pointed at the producers. **(2) Before writing a fresh encoding of a scope field, grep the same file for an existing correct one:** `createSeedPolicyEntries`'s §6.9a self-owner entry has carried the dual form `{"*", "/*/*"}` since v0.8.0, ~160 lines away in `core/peer/peer.go`, and the default self-grant never inherited it — the same sentence rust wrote about their own `debug_open_grants` R-5 note. Method note worth keeping (A1, trace before you theorize): this was answered by an **instrumented denial** — a real peer, a real handler, printing the ceiling's canonicalized pattern beside the target — not by reading `MatchesPattern`, and go's full suite was green for five days across the defect. Fixed: `defaultHandlerSelfGrant()` (peer-wildcard `/*/*`), teeth `core/peer/handler_grant_ceiling_test.go` — mutation A (restore bare `*`) reds the foreign-namespace row alone, mutation B (neuter D1's `Resource:`) reds both narrow-deputy controls, so "widening the default" and "putting a hole in D1" are pinned in opposite directions. **Peers is deliberately NOT widened**: §5.2 Dimension 4's absent→`{include:[local]}` is *correct* for a self-grant (the peer under test is `extract_peer` of the dispatch TARGET, not the namespace being written), so "completing" the fix there is an escalation, and a test says so.
  > **Third axis, 2026-09-11 (RATIFIED — an exclude-semantics fix's blast radius is a MATRIX, not a site, and BOTH siblings found go's incomplete sweep, the two-siblings-agree form of "the cross-impl FAIL is the true signal").** go's first H1 pass (`77380ba`, 0.8.2.21) fixed `isExcluded` — ONE exclude-reading site — and routed *"0.8.2.21 absorbed, nothing owed back"* (`ROUTING-2026-09-11-e`). It was owed at FOUR more coordinates, and rust and py **independently** found the identical set: (G-1) `CheckPathPermission`'s resource-exclude loop had no NEVER_MATCH arm — the §6.3 site that IS the enforcement once the dispatch resource dimension is vacuous (0.8.2.20), live via `handler.go:386` and every tree/content/compute/history/subscription/httplive caller; (G-3) the *handlers* dimension dropped exclude entirely at BOTH read sites (`scopeContains` dispatch + `CheckPathPermission`); (G-2) the validity walk `FirstUnmatchableScopePattern` walked resources but not handlers; (G-4) `CheckResourceScope` had no §5.2 pattern-target arm, so a pattern target re-spelled straight past a concrete grant exclude (`system/tree/*` bypasses an exclude of `system/tree/secret`). Plus rust's listing/extract gap: EXTENSION-TREE §8.2's per-entry `can_get` filter was unimplemented, so a listing/extract shipped every entry under a covered prefix regardless of the cap's excludes. The blast radius of an exclude-semantics / path-scope rule is **{every exclude-reading site} × {both scope dimensions: handlers AND resources} × {both target shapes: concrete AND pattern} × {three consumer surfaces: dispatch, §6.3 handler-level, listing/extract per-entry}** — a fix at one coordinate is 1 of a 2×2×N matrix, and go-on-go stays green across the empty cells because go's own callers never author the excluded shape. **Two enforcement points, both re-applications (no novel discipline — the "enumerate every surface a rule names / sweep the class not the instance" family + "the cross-impl FAIL is the true signal," recorded per the close-out floor): (1)** fixing a fail-open at one exclude-reading site obliges a grep of the whole matrix — `git grep -n 'Exclude' core/capability` for every read site, and confirm BOTH scope dimensions (handlers via `scopeContains`, resources via `CheckResourceScope`) and BOTH target shapes carry the arm, and the per-entry consumers (`handleListing`/`handleExtract`) filter. **(2)** *"nothing owed back"* on a SECURITY ruling is a claim about the whole matrix that go-on-go cannot verify — declaring the board closed on go's local view while sites sit open is the "board closed on one party's view" process defect (registry drip-feed audit), and here it cost a full extra cohort round; do not route "absorbed, nothing owed" on an authz sweep until the matrix is grepped AND driven cross-impl. Fixed `<this commit>`: G-1/G-3 (`CheckPathPermission` + `scopeContains` NEVER_MATCH + handler-exclude), G-2 (`FirstUnmatchableScopePattern` walks handlers), G-4 (`CheckResourceScope` concrete/pattern split, spec §5.2), §8.2 filter (`handleListing` filtered `count`, `handleExtract` re-roots over filtered bindings). Teeth: `core/capability/h1_scope_test.go` (4 tests, each with a discriminating well-formed control) + `core/tree/listing_extract_filter_test.go`, all six mutation-verified RED against `77380ba` via `git stash`.
  > **The matrix bit AGAIN one round later (2026-09-12) — the "consumer surfaces" and "read sites" axes were UNDER-enumerated by three, and both siblings found go's remaining cells by re-running the grep the ratchet above prescribes (the two-siblings-agree signal, second occurrence → the enforcement is sharpened, not re-derived).** My matrix (above) listed the consumer axis as "{dispatch, §6.3 handler-level, listing/extract per-entry}" — three surfaces, chosen by intuition. There were more: (rust) the **query** handler's step-6b filter (`checkQueryPathPermission`) consulted the request's `Resource` field and returned true when ABSENT — the bulk-read enumeration surface, and it never called the capability at all (a caller-request narrowing masquerading as authorization); (py) **snapshot** built its trie over every binding under the prefix, and since §11 exempts `diff` from path checks, `snapshot + diff-against-empty` leaks the excluded key and its content hash — the widest metadata surface; (py) **`CheckPathPermission` itself has the G-4 pattern hole** on its own axis — EXTENSION-SUBSCRIPTION §2.3 hands it a PATTERN subject (`data/*`), and the concrete-only exclude test let it re-spell past a concrete grant exclude, so an `include_payload` subscription shipped the excluded body on every write, forever. So G-4 had TWO sites (`CheckResourceScope` AND `CheckPathPermission`), and the consumer axis is not a fixed list of three — it is **every handler that authorizes a path against a cap**, enumerated by `git grep -n 'CheckPathPermission(' core/ ext/` (tree get/put, content, history, subscription, compute, httplive, query) PLUS every BULK reader or COMMIT surface (listing, extract, snapshot, query results). **Two enforcement points: (1)** the read-sites/consumer axis of a path-authorization sweep is enumerated by the `CheckPathPermission(` call-site grep and the bulk/commit-surface list, NOT by intuition — a security-sweep close-out names the ENUMERATION METHOD and its RESULT ("grepped N call sites, all filter"), not "swept," so the next seat sees which cells were checked; **(2) NEW candidate — a path-check EXEMPTION is a claim about its UPSTREAM producer.** `diff` is exempt from path checks (§11) ONLY because a snapshot is supposed to commit to nothing the caller cannot see; the exemption was sound in prose and false in fact because snapshot was unfiltered. When you exempt an operation X from an authorization check, name every operation that COMMITS the data X consumes (here: snapshot → the root diff reads) and confirm IT filters — an exemption that rests on an unenforced upstream invariant is a hole with a rationale. First bite 2026-09-12 (py's snapshot/diff drive). Fixed `<this commit>`: query consults the cap (`CheckPathPermission("get", …)`, request-resource demoted to an optional narrowing), snapshot per-binding filtered with the tracked-root fast path bypassed under a scoped cap (a filter on only the rebuild branch would be skipped through the O(1) path — py flagged that too), `CheckPathPermission` gains the §5.2 pattern arm (overlap→deny, no caller-exclude at that site). Also corrected: the listing/extract citation was EXTENSION-TREE §8.2 (View Trees, §12.2-scoped to peers that implement them — the citation under which a no-view-tree seat concludes the obligation does not bind it); the unconditional rule is core §6.3. Teeth (all three mutation-verified RED against `b8ee9e5`): `TestCheckPathPermission_PatternSubjectHonorsOverlappingExclude`, `ext/query.TestCheckQueryPathPermission_ConsultsCapabilityNotRequest`, `core/tree.TestSnapshot_FiltersExcludedBindingsFromRoot`.
  > **The matrix bit a THIRD time, 2026-09-13 — the SENTINEL cell at EVALUATION (0.8.2.22 J1/J3, arch ROUTING-2026-09-13-d; no new discipline, recorded per the floor).** The 2026-09-12 pass added the §5.2 pattern arm to `CheckResourceScope` (G-4) and `CheckPathPermission` — the OVERLAP→deny half — but wrote the unmatchable-exclude sentinel arm one line too LATE in both: `PatternsOverlap` `continue`s on a sentinel, so an unmatchable grant exclude (`*/secret` → NEVER_MATCH) skipped the deny and the pattern target/subject was ALLOWED (the fail-OPEN direction of the sentinel). The concrete arms had it via `isExcluded`; the pattern arms deferred it to `FirstUnmatchableScopePattern` at MINT, and 0.8.2.22 is the ruling that a received cap not minted locally reaches EVALUATION, so the sentinel MUST also deny there — "a control fails closed on its own input at the evaluation site, not only at authoring." Fixed `<this commit>`: `IsUnmatchablePattern(cge)`→`return false` before `PatternsOverlap` in `CheckResourceScope`'s pattern arm (J1), and `IsUnmatchablePattern(cexcl)`→`excluded=true` before the overlap test in `CheckPathPermission`'s `IsPattern` branch (J3). Teeth: `TestCheckResourceScope_PatternTargetUnmatchableGrantExcludeDenies` + `TestCheckPathPermission_PatternSubjectUnmatchableExcludeDenies`, both with a well-formed-exclude ALLOW control, mutation-verified RED (remove the arm → the overlapping row flips to ALLOW). The matrix's cell list gains a fourth axis: for each {exclude-reading site}×{scope dim}×{target shape} the sentinel arm is a control-flow OBLIGATION (before the overlap/match test), not a line — grep each pattern arm for `IsUnmatchablePattern` preceding `PatternsOverlap`.
  > **Two K1-wire-probe harness lessons, 2026-09-13 (the `resolution_integrity` category).** **(a) A wire forgery / negative-authz probe must be constructed so EVERY pre-existing guard PASSES it and ONLY the new guard refuses — and mutation-verified by DISABLING the new guard and confirming the forgery is ACCEPTED (200), not merely that it is refused (re-application of "a negative authz test can pass for the wrong reason", 0.8.2.19(b), on the wire-forgery axis).** My first author-forgery construction signed with the attacker's key AND named the attacker as the signature's `Signer` — so `signer != author` caught it, redundant with the existing `signer_author_mismatch` check, and it would have read PASS against a binding-DISABLED peer (refused for the wrong reason). The isolated K1 vector sets `Author = victim` AND the signature's `Signer = victim` (via `signEntity(execHash, attackerKP, victimIdentity)` — claim victim, sign with attacker), with `included[victimHash] = attackerIdentity`: now `signer==author`, the attacker's own key verifies the signature, and NOTHING but the map-key binding distinguishes accept from refuse. Proven by driving a peer with all three binding guards neutered → forgery ACCEPTED 200; restored → refused. The signer-mismatch control (same signature, CORRECT victim identity filed) stays refused in both, anchoring attributability. **Enforcement: a forgery/substitution probe names the guard it isolates, shares every other input with a positive control, and its teeth are a peer-side mutation that makes it ACCEPT — a probe that only ever observes "refused" cannot tell the guard-under-test from a sibling guard.** **(b) NEW candidate — a validate-peer category whose expected refusal is a receive-boundary CONNECTION CLOSE must run on a DEDICATED `newClient` connection, never the shared suite connection.** go binds the included map at `validateRecv` in `serve()` (core/peer/connection.go) and CLOSES the connection on failure (the deferred `c.Close()`), rather than emitting a coded §5.2a DENY — fail-closed, the pre-existing behavior for any receive-time hash-validation failure. On the SHARED pass-1 connection that close cascaded EOF into EVERY later category (exclude_matrix, attestation, quorum, … — 20+ unrelated categories RED, `PASS 1 exit 1`), and the per-category `FAIL` lines carried no detail, so it read like a broad regression. Fix: `runResolutionIntegrity` takes `s.newClient`, connects a sacrificial client, runs all arms there (forgery last), and `Close()`s it — the close is isolated. **Enforcement: if a category's conformant outcome INCLUDES a connection close (a receive-boundary/decode-boundary refusal, an intentionally-corrupt frame), it takes a dedicated connection; a close on the shared connection is a false cascade, and the tell is a contiguous block of unrelated categories failing from one line onward with empty detail.** (The separate finding — whether §5.2a's decode-boundary row MUST return a coded 400/401/403 rather than a bare close, per §4.1 "every EXECUTE receives a response" — is decision-gated and routed to arch, not a go discipline.)
- **A bare `validate-peer` run is NOT the release gate — surface-gated categories FAIL as peer-POSTURE artifacts, not defects (learned 2026-08-18, cost a chunk of a session).** `registry_issuer` returns `501 unsupported_operation` and `serving_mode` returns `404` against a peer started with a plain `peer-manager start`, because those categories require the peer to be started IN a posture (an armed issuer policy; a converged closure/namespace serving root). The release gate is `scripts/validate-complete.sh`, which starts the peer with every surface armed and scores each posture-gated category in its OWN pass (registry_issuer 31/31 in pass 3 as of 2bd2380 — the v1.11 TTL-ceiling vectors added three, serving_mode 55/55 in pass 2). **Before routing a raw-run FAIL as a sibling's — or our own — conformance defect, classify it against peer posture: a category that needs a startup flag the bare run didn't pass is an unwired surface, and its FAIL/501/404 is the artifact of that, provable by re-running under `validate-complete.sh` (or a targeted repro with a settle).** Same family as ADR-0012 green≠correct, inverted: red≠defect when the harness never armed the surface. (I nearly filed go's own `registry_issuer` 501s and `serving_mode` 404s as regressions; both were posture/timing, the peer was conformant.)
- **A scheduling race is invisible on a multi-core box — the gate must run the suite STARVED (`make test-starved`, GOMAXPROCS=1, repeated), because `make race` stays green on a lost UPDATE (candidate, first bite 2026-08-23, `fb6d461`; browser-rust's release gate surfaced it, and only on a loaded machine).** `ext/publishedroot`'s undebounced publisher spawned one goroutine per tracked-root advance, each carrying a CAPTURED root hash; under contention those reorder and a late goroutine holding an OLD root could publish it LAST, stranding the published root behind the tracked root — a §6.5.6 convergence FAILURE. It passed `make test`, `make race` (it is not a data race, so the detector is green on it), all 45 curated commits, a standalone canon-filtered run, and a fresh clone — every one on an unstarved `--cpus=8` box, where async goroutines schedule in near-order and it converges every time. It failed exactly once, on a loaded machine during a gate run. The measured tell was `seq > writes` with `converged=false`: the work was done, the WRONG root landed last (a lost update, not the "dropped tail / lost wakeup" the test's own message theorized — trace the value, A1). **Two enforcement points, both landed: (1)** the specific regression is pinned deterministically by `TestOnTreeChangeRecordsNewestInSlot` (asserts the coalescing slot holds the NEWEST advance and the drain publishes it — 100%, runs in plain `make test` on any box); **(2)** the general class is netted by `make test-starved` — `GOMAXPROCS=1 go test -count=N` across all modules, the lever that reproduces this on demand (~3/8 at count=1 pre-fix). Run it as a release-gate step ALONGSIDE `make race` and `validate-complete.sh`; the two detectors are orthogonal — starvation exposes reorder-dependent logic races, the detector exposes memory races, and neither sees the other's bug. When you add async coalescing/debounce/hook-fanout code, add a deterministic newest-wins/convergence test AND lean on `test-starved`; a green multi-core suite is not evidence a scheduling race is absent, it is evidence the scheduler hid it.
- **An async-on-contention DEFER creates a stale-READ hazard for every synchronous consumer of the state it defers — a distinct shape from the lost-UPDATE above, and one that GOMAXPROCS=1 starvation HIDES because it needs real parallelism (ratified 2026-08-30 — second bite of the `go apply(...)`-behind-a-`TryLock` class; workbench-go's burst reproducer surfaced it, ten days after it was written).** `core/tree.RootTracker.applyEventWithDepth` spawns the incremental root apply to a goroutine when the per-prefix lock is contended and returns before it lands, so `OnTreeChange` reports done while the cached `system/tree/root/{prefix}` still lags the live index. The AutoVersioner runs strictly AFTER the tracker on the same event *"so the tracked root is settled before we read it"* — but the defer breaks exactly that invariant: `fire()` read `tracker.Root()`, got a root missing the triggering (last-of-burst) write, built a deficient `versionRoot`, and the dedup check silently no-op'd the emit. Terminal, because a burst's last write has no next event — the *"symmetric last-burst-write loss."* Traced, not theorized (A1): under load with `ENTITY_AV_TRACE`, the failing run printed `[root-tracker] ASYNC-SPAWN … path=…/a-4.md` for the exact lost path, then `fire … dedup … no emit` — **zero CAS retries, zero no-tracked-root**, refuting the source comment that credited the CAS loop with closing this. It was invisible three ways the publishedroot bug was too (`make test`, `make race`, a fresh clone) PLUS one more that is the new teeth: **`make test-starved` (GOMAXPROCS=1) actively SUPPRESSES it** — with one runnable goroutine the spawned apply drains before `fire()` reads, so the settle-race never opens. Starvation nets the lost-UPDATE reorder; only real parallelism nets this stale-READ. **Fix + enforcement, both landed:** the tracker counts in-flight async applies per prefix and exposes `CurrentRoot`, which recomputes from the live index (authoritative, monotonic under a write-only burst → cannot regress or phantom-delete) whenever an apply is pending and keeps the O(1) cached-root adoption otherwise; `fire()` reads through `CurrentRoot`. Teeth: `core/tree.TestRootTracker_CurrentRootReflectsPendingAsyncApply` is DETERMINISTIC (holds the prefix mutex to force the defer, asserts `CurrentRoot` ≠ the lagging `Root`; mutation-verified RED when `CurrentRoot` delegates to `Root`), and `ext/revision.TestAutoVersion_CapturesEveryWriteUnderConcurrentBurst` is the parallelism net (RED on iter 0 pre-fix under `-race`). **Enforcement, general: when you add an async-on-contention path (`go work(...)` behind a failed `TryLock`), `git grep` every reader of the state it defers and confirm each is safe under the lag — a read-through that recomputes, or a pending-guard; a synchronous consumer that trusts "the hook ran" reads stale.** And add a DETERMINISTIC forced-interleave test (hold the lock, assert the read-through), because neither `-race` nor `test-starved` will catch a parallelism-gated stale read — the third detector this repo now owns. (Same finding also flags `ext/publishedroot/publisher.go`'s `tracker.Root` read as sharing the shape — reported to meta, not fixed here: it republishes on later events so it is not terminal.)
- **Converging an error CODE, the conformance unit is the status SLOT — the SET of spellings emitted at that status — never the single token an incident named (candidate, first bite 2026-09-03, arch's 0.8.2.7 slot ruling caught it).** `0a6d3c9` swept `400 unknown_operation → 501 unsupported_operation` and closed out the 501 row as done; five other 501 spellings the incident did not name survived — `not_implemented` ×6 in go (`execute.go:293,667`, `local.go:454`, `compute/handler.go:137,144`, `peerissued/register.go:391`), plus `not_supported`/`unsupported_mode`/`not_available` cohort-wide — so the slot stayed divergent while the sweep reported complete. A token-scoped sweep is done-looking and wrong: a client branches on the code, and two spellings at one status IS the divergence whether or not the incident named both. The identical shortcut produced the spec's false "three implementations converged on `internal_error`" (0.8.2.6) — it measured one spelling instead of censusing the slot, and "N converged on X" is a positive-sounding claim whose truth condition is a negative, so the prove-a-negative rule never fired (arch's AP-21/L8). Enforcement: when you converge an error code, census the whole slot FIRST — `grep -rhn '<status>, "' core/ ext/ | grep -oE '<status>, "[a-z_]+"' | sort | uniq -c` — and converge every generic-case spelling to the spec-named default (leaving only codes DEFINED in a spec code set); a sweep close-out quotes the slot census, not the one string it changed. Same family as "enumerate every surface a rule names," one axis over: there the surfaces a rule touches, here the spellings that occupy one status slot.
  > **The layer BELOW the slot — the code must live in the `code` FIELD, not merely be a field carrying the right spelling (candidate, first bite 2026-09-03, a go harness fix caught core-rust).** The slot census above counts `(status, spelling)`. It is blind to a code carried under the WRONG KEY: core-rust's type-system handler answered `501` with `{type: "unsupported_operation", message: …}` — right status, right spelling, wrong field — from a local `error_entity` (`extensions/type-system/src/{validate,constraint}.rs`) that shadowed the canonical `core/handler` one (key `code`). A conformant client reading `result.data.code` (§3.3 `system/protocol/error := {code, message}`) gets `""`; a `grep` of the raw error map finds the string under `type`; go-on-go never sees it (go emits `code`). This survived 0.8.2.5→0.8.2.7 and two cross-impl sweep rounds because no unit anyone counted could see it — the same "census keyed on the wrong axis" root as the registry drip-feed, and the same "shadowing local helper diverged from canonical, nothing compared them" meta-shape as `globMatch`/`matchPattern`. Enforcement: the conformance unit is `(status, code-read-from-the-`code`-key)`; a §3.3 error-code check reads the code via the `code` field ONLY and FAILs a code found under any other key — never a raw-map fallback that reads any key, which launders the defect (`decodeResultErrorCode` reads `code` only, by design; `classifyOptionalTypeOp` FAILs the empty-code case rather than reading `type`). Method (A1, trace the value a real client reads): the tell was `result.data.code=""` under `decodeErr=nil, resultLen=156` — a successful decode with an empty read points at the field key, not a decode bug. Teeth: `cmd/internal/validate.TestClassifyOptionalTypeOp` (`not_impl_501_empty_code_fails`, mutation-verified — dropping the code guard reds it). **A code is `(status, field, spelling)` — three axes, and a census that fixes two is blind to the third** (core-rust promoted the same triple in their charter). **The enforcement point must name the MINT-SITE region, not the call site: an impl-local `error_entity`/error-mint helper that shadows the canonical one is invisible to a call-site grep** — which is exactly how this survived three spec revisions and two cross-impl sweeps (core-rust confirmed: `4d888f8` had just landed `unsupported_operation` at those very sites, so the slot was right, the spelling was right, the field was wrong, and every census counted the 501 row closed). Enumerate every mint site of the substrate error entity and extract the field key at each — core-rust did this and found exactly 2 defective of 25, both the shadowing helper's; a grep of the constructor (`error_entity(`, `cbor_map!`, the `{code:` literal) across the whole crate, not the dispatch call sites, is what surfaces a divergent local mint.
  > **The ROUTING axis of the same law, and it bit us both directions at once (candidate, 2026-09-05, core-rust caught it): when you ROUTE a sibling's error-CODE defect, the evidence is the OBSERVED `result.data.code` from a DRIVE — never a `git grep` of the code TOKEN, because the token can sit in the human MESSAGE while the dispatcher/wrapper supplies a different, possibly-DEFINED code.** Cycle 4 routed rust two tree:put lines as an `invalid_entity` defect, grepped from their source. Both were `HandlerError::InvalidParams(format!("invalid_entity: {e}"))` — the token was in the *message*; the dispatcher emitted `invalid_params` (a DEFINED §3.3 specific). So the ledger was wrong twice over: rust never shipped `invalid_entity` (closing the item as titled would assert a defect they did not have), and the real defect was *worse* than the one named — `invalid_params` passes any grep for undefined tokens while still costing the caller the branch the two-row split exists to give. The one real `invalid_entity` EMISSION was in `content:ingest`, a handler the token-grep of tree code never looked at. Two enforcement points. **(1)** A routed sibling error-code claim cites a driven `(status, code)` pair or is marked "unverified — drive before acting"; a claim whose evidence is `git grep <token>` is not evidence of the emitted code, and the unverified code MUST NOT go into the ledger/table AS the defect (hedging in prose, as cycle 4 did, is not enough — the table still asserted it). **(2)** A source read of a sibling's error codes is blind to an UNCAUGHT-exception path: driving the new `put_undecodable` wire check surfaced py answering `500 internal_error` for a non-decoding put entity (a client-triggerable 500 where EXTENSION-TREE Appendix A v4.4 row 1 mandates `400 invalid_request`) — a 500 that NO error-code string in py's source produces, so no grep could have found it; go and rust both return `400 invalid_request`, so the cross-impl FAIL is the true signal. Same triple `(status, field, spelling)` as the block above, now on the routing axis: read the code a real client reads (drive it), not the token a grep finds. This is why the tree:put row is verified by a wire check driven three ways, not by three source reads. Re-application of "the conformance unit is the code read from the `code` key" + "the cross-impl FAIL is the true signal"; recorded because it bit on the routing axis and cost a doubly-wrong ledger, corrected in report `docs/validation/reports/2026-09-05-b-*`.
- **A value routed through a shared derivation function is honored ONLY where the input reaches the function intact — a dispatch-time struct REBUILD that copies some fields and drops others silently defeats the function, and the unit suite is structurally blind to it because the unit calls the function with a full struct (candidate, first bite 2026-09-11 — my OWN wire vector caught it; the F68 `effective_targets` fix).** Landing `capability.EffectiveTargets` (§5.2, 0.8.2.20) as the single subject-derivation point for the authorizer (`CheckResourceScope`) and every handler (`hctx.ResourceSubject`/`ExtractResourcePath`) was unit-green, vet-clean, race-clean — and INERT on the wire, because `normalizeResourceTargets` (`core/protocol/dispatch.go`) rebuilt `types.ResourceTarget` as `&types.ResourceTarget{Targets: ...}`, dropping the caller's `Exclude`. So the handler's `hctx.Resource.Exclude` was always empty, `EffectiveTargets` skipped nothing, and the self-excluded (a) and case-D (b) WIRE vectors answered 200/403 where they must answer path_required / read-Q. Every unit test PASSed because they call `EffectiveTargets`/`ResourceSubject` directly with a fully-populated struct — the drop happens only on the dispatch path a unit never exercises. This is the exact wire-only sibling of the two-layer drift `effective_targets` was built to close: the fix needed a THIRD site (the dispatch normalizer) the design named only two. Enforcement: when you route a value through a shared function that reads a struct field, `git grep` every site that CONSTRUCTS or REBUILDS that struct on the dispatch/handler path (`normalize*`, cloners, `&types.X{...}` field-by-field copies) and confirm it preserves EVERY field the function reads — a field-dropping rebuild defeats the function silently; and prove the fix with a WIRE vector (positive AND negative), because a unit test handed a full struct cannot see a producer that drops the field. Same family as "Go-on-Go passes deceptively; the wire/cross-impl drive is the true signal," on the axis of a struct-rebuild that starves the shared function of its input. Teeth: `resource_effective.resource_effective_self_excluded_path_required` + `resource_effective_case_d_witness` (both mutation-verified RED against the exclude-dropping normalizer AND against a handler reading `Targets[0]`).
- **A defense-in-depth `panic`/assert guarding a path (or any value) is only defense-in-depth if that value is validated on EVERY channel that reaches it — a handler that derives a SECOND path from params is a channel the resource-target pre-validator does not cover, so the assert becomes a remote DoS (candidate, first bite 2026-09-11 — rust's panic-shape grep routed the class; the reachability was CONFIRMED, not assumed).** `core/store/namespaced.go:canonicalize` panicked on a malformed path under a comment asserting *"external input is validated at the dispatch layer before reaching any handler."* False: the dispatch pre-validator (`core/protocol/execute.go`) validates only `Resource.Targets`, and `system/tree:extract` (`paths[]`) and `:merge` (`target_prefix`+source-envelope bindings) hand a caller-controlled PARAMS path straight to the location index — so a control char or non-peer segment in params reached the assert and unwound the connection task, for ANY authenticated peer holding a normal grant (extract unconditionally; merge with a wildcard `put` grant). The tell it was reachable, not theoretical: a subagent traced the concatenation `fullPath := prefix + relPath` with `relPath` from unvalidated `extractReq.Paths` to `LocationIndex.Get` → panic, with the DispatcherLocationIndex confirmed to be a `NamespacedIndex`. **Two enforcement points. (1)** `git grep 'panic('` in any store/path/index/canonicalizer code and, for each hit, ask *can a caller-controlled value reach this?* — the answer is not "the dispatch layer validates it" unless you have confirmed the dispatch layer validates THIS channel; a params-carried path is a DISTINCT channel from the resource target and the resource-target pre-validator says nothing about it. **(2)** the fix is to make the boundary TOTAL (rust's exact "make the boundary function total, not an assert" shape, applied to go's store): a malformed path is untrusted input to REFUSE, not a programming error to assert on — reads fail closed to absent, writes return an error, the handler shapes the 400. Same family as "a control MUST fail closed on its own config" and rust's `qualify_path`-total fix; the new axis is *which channel carries the value the assert guards*. Teeth: `core/store.TestNamespacedIndex_TotalOverMalformedPaths` (boundary total, mutation-verified) + `core/tree.TestExtractMalformedPathIs400NotPanic` (handler 400, and a green run IS the no-panic proof).
- **An unmatchable/sentinel value's safety is DIRECTIONAL: fail-CLOSED in an include/allow position, fail-OPEN in an exclude/deny position — so a matcher that returns false for it silently WIDENS a grant when the pattern sits in an exclude, and the whole cohort shipped it (ratified 2026-09-11 — arch shipped the fail-open in 0.8.2.20 over rust's fail-closed reading, corrected it cohort-wide in 0.8.2.21 H1; go had it).** §5.4 canonicalizes `*/`/`./`/`../`-leading patterns to NEVER_MATCH, and `matches_pattern` returns false for it. In an `include` that is fail-closed (covers nothing → grants nothing). In an `exclude` it is fail-OPEN: carves out nothing → the grant is silently wider than its author wrote, with no error anywhere (a granter writing `exclude: ["*/secret"]` gets an exclusion that excludes nothing). go had exactly this: `isExcluded` ran the unmatchable exclude through `MatchesPattern` → false → not excluded → allow. **Two enforcement points, both required by the spec as TWO LAYERS because a single layer author-or-caller input can make vacuous is not a gate. (1) Evaluation:** an exclude/deny arm MUST treat an unmatchable pattern as matching EVERYTHING (deny), never defer to the matcher's false — `git grep` every exclude/deny-position use of the shared matcher (`isExcluded`, `matches_scope`, any `Exclude` loop) and confirm the unmatchable case is handled explicitly. **(2) Authoring/verify:** a capability any of whose scope patterns (include OR exclude) canonicalizes to the unmatchable value is INVALID `[MUST]` — refuse `400 invalid_path` at mint/delegate (§6.2) and treat as invalid at verify (§5.5), because the two readings (refuse vs honor-a-wider-grant) are **different authorization decisions on the same bytes across a peer boundary**. It is a MUST, not a MAY, for exactly that reason. Same family as "a control MUST fail closed on its own config" and rust's "make the boundary function total, not an assert," on the new axis of *which side of a set-membership test the unmatchable value sits on*. The discriminating test control is a WELL-FORMED exclude beside the malformed one (`/*/secret` denies `/{peer}/secret` but not `/{peer}/public`), or the row cannot tell a working exclusion from a grant that denies everything. Teeth: `core/capability.TestCheckResourceScopeUnmatchableExcludeExcludesEverything` (eval, mutation-verified) + `TestFirstUnmatchableScopePatternDetectsAllForms` (validity, all three forms). The wire-drivable vector `CORE-EXCLUDE-UNMATCHABLE-1` is keystone's per arch's assignment.
- **Diagnostics first** (`docs/architecture/guides/USING-DIAGNOSTICS.md`): when something fails, reach for `validate-peer -category $CAT -verbose 2>trace.log` + the peers' `~/.entity/logs/{name}.log` **before** reading code or writing analysis. Wire evidence is upstream of theory. Never `-verbose` the full suite to a TTY (~130 MB) — always narrow with `-category` first. For in-process Go work, use `peer.WithDispatchHook` / `WithWireHook` / `WithBindingHook` for tap observability without rebuild churn. (Negative-path tests like `security`/`multisig.escalation_*` legitimately emit 403s in the responder log — map `request_id` → test name.)
- **Never ad-hoc-filter validate-peer output.** Use `-failures-only`, `-exclude`, `-category`, `-json` — never a throwaway `python3 -c` / shell script to tally or "confirm" the tool's own output. The summary table already gives per-category P/W/F counts; to compare peers, run it N times and read the tables. (Corrected repeatedly — don't reach for `json.load` to re-verify what `-failures-only` already shows.)
- **A conditional assertion inside a check can silently no-op, and the summary PASS row cannot tell you it ran (ratified 2026-08-17 — a peer corrected around it).** `request_ttl_zero_and_overflow`'s no-ceiling probe fires only when the connection cap is nil-expiry, and the CAP-6 `ingest_rejects_unrepresentable_expiry` control runs only if the round-tripped normal-expiry token is honored — a skipped branch and an exercised one both print `PASS`. core-rust, validating the `(u)` check, *"checked the check's own message rather than the PASS row"* to see whether the branch ran, and said so. **Enforcement, both halves: (1) a conditional probe MUST report in its own PASS message whether the conditional branch executed** (as these two do: `"; no-ceiling overflow minted NO expiry…"`, and the control's honored-first gate that otherwise `SkipCheck`s), so a no-op degrades to a visible SKIP, never a silent PASS; **(2) verifying a check against a sibling, read the per-check MESSAGE, not the summary PASS/row-count.** Same family as ADR-0012 (green ≠ correct if the assertion didn't fire).
- **When an oracle check cannot be teeth-tested against a failing peer, carry the teeth IN the check (2026-08-17).** The usual proof — run the check against a pre-fix commit and watch it go RED — is blocked when no failing peer is buildable: go never had the defect, and the git boundary forbids checking out a sibling's pre-fix commit (`entity-core-{py,rust}`). The CAP-6 ingest check instead embeds a **negative control**: a round-tripped *normal*-expiry token must be **honored first**, proving the construction reaches the expiry check, so a refusal of the hostile variants is attributable to the expiry rather than to a corrupted granter/chain; if the control is refused the check **SKIPs, not PASSes**. A hostile-input check whose valid-input control is never asserted is a candidate false-pass. (This is ADR-0012's mutation-has-teeth applied to a wire check — not a new invariant, a new place to apply it.)
- **A wire probe reaches a STATE-INDEPENDENT `[MUST]` only in the cheap-to-reach state, so an "in any state" clause is driven in exactly ONE state cohort-wide and no cross-impl run catches a regression on the arm nobody dials into — a probe DIALS IN, and reaching the expensive state (established, mid-handshake) is written as SETUP rather than as a state under test (candidate, py earned it 2026-09-02, ROUTING-2026-09-02-b).** §4.7 row 10 (an unknown connect op → `400 invalid_request`) is "in any state," but every seat's `connect_unknown_operation` probe sends the unknown op on a FRESH connection, so the established arm went untested in all three harnesses; py's established boundary was actually wrong there (it answered `409` for the connection's STATE when the defect was the operation's NAME), and no cohort run could have said so because the cohort never drove that arm. go was already correct (`execute.go`: the unknown-op switch runs AHEAD of the state branch), so **this bit go's COVERAGE, not its behavior** — hence candidate, not ratified here. Enforcement: for a state-independent MUST, drive it in the EXPENSIVE state too and carry an in-probe control that the expensive-state branch still discriminates (here `connect_unknown_operation_established` sends `frobnicate` on an established connection → `400`, with a control that a `hello` in the same state → `409 connection_already_established`; if the control does not hold the branch is degenerate and the check WARNs rather than PASSes). Same family as carry-the-teeth: the arm you do not exercise is the arm that ships wrong. Teeth: `core/protocol.TestFold0902_UnknownConnectOperationIsInvalidRequestInAnyState` (mutation-verified — moving the unknown-op switch below the state branch reddens step 1). A related applicability control landed the same session: `connect_ping_before_hello`'s "peer must serve ping" caveat is now ASSERTED (`pingServedOnceEstablished`) instead of prose, so a peer that does not serve ping SKIPs the row rather than the `409` quietly failing a peer that conformantly answers `400`.
- **An impl-agnostic oracle check we author is mutation-verified ONLY on the seat we mutated — on every other seat it is an unarmed CLAIM, and a spec-field oracle makes it PORTABLE, not ARMED (candidate, first bite 2026-08-31, py caught it).** We shipped `auto_version.burst_convergence_no_capture_loss` reading a spec field (REVISION §6.1 `status.pending`), verified its teeth honestly by reverting go's fix and watching it go RED, and reported it impl-agnostic. It was — and it was **blind on two of the three seats it was built to run against.** `pending` is computed only `if !headVal.IsZero()` (`ext/revision/status.go`, and the same guard rides every impl), so a peer that captured NOTHING for the burst prefix mints no head, reads `pending=0`, and PASSes the check whose entire purpose is that loss. go's own defect leaves earlier captures — and therefore a head — behind, so the go mutation armed the arm that already worked; the total-loss arm was never exercised by the go teeth. py proved it by mutating a py peer to stop capture outright: still 16/16 PASS. **The general law: reverting the fix on the authoring seat verifies the check against the authoring seat's failure MODE, not against the failure the check names on other seats — a portable oracle can be blind on a peer whose defect reaches a different arm of the same field.** Enforcement: when you author a cross-impl oracle check, enumerate the DISTINCT failure modes the named condition can hide (here: partial loss = head + uncaptured path, vs total loss = no head at all) and arm the check against each, not only the one the authoring seat exhibits; a spec-field oracle proves portability, and the mutation on your own seat proves only the arm your own bug reaches. Fix: convergence now requires `pending==0 AND head present` (`cmd/internal/validate/auto_version_burst.go`), closing the blind arm WITHOUT touching `status.pending`'s no-head semantics — that case is unruled (py SA-PY-28) and changing it would unilaterally converge go onto py's reading of the absent case, which the divergence rule forbids. **No new discipline for the fix itself** (it re-applies the fail-closed-control rule and the divergence rule); the earned line is the portability≠armedness law above. Owed onward: SA-PY-28 (arch rules the no-head arm; if go adopts py's reading the check is armed on all three seats with no further change), and rust's `guarded_publish` last-advance drop, which rust filed as found-not-fixed (same shape as our `ext/publishedroot`).
  > **Second bite, 2026-09-04 — the COVERAGE axis of the same law: a cross-impl oracle authored for a rule that spans N operations, scoped to the ops the AUTHORING seat's own measurement flagged, is blind exactly on the ops where the divergence is live on OTHER seats — and go-on-go cannot see the gap because go is conformant on all of them (rust + py both caught it).** The type-op 404 gate (`cmd/internal/validate/typeext.go`) probed only `converge`/`adopt`/`reconcile` — the three §12.2/§12.3 MAY ops go's own §3.3 measurement had named — and skipped `compare`/`compatible`, the §7.2/§7.3 SHOULD ops all three seats build and the row where PROPOSAL §6a.3's `not_found`→`type_not_found` narrowing is ACTUALLY live. So rust's and py's row-4 fix was cross-impl-invisible: rust's own `type` score was byte-identical before and after their fix, which is why rust asked go to EXTEND the gate rather than report it verified. Same law as the arm-blindness above, one axis over: there it was the *arms of one field*, here the *operations one rule spans*. **Enforcement: read the op-scope of the rule from the SPEC/PROPOSAL text itself (§6a.3 governs row-4 AND the MAY ops), enumerate every operation it names, and probe each — not only the ops your own seat's incident surfaced; the tell you under-scoped is that YOUR seat passes every probe while a sibling's live divergence sits on an op you did not add.** Companion defect the same review (rust caught): the check `SkipCheck`'d a conformant not-implemented `501`, and a SKIP counts as a failure — so a rust peer that builds 3 of 6 ops exited the suite 1 with zero failing rows, while the roundtrip row above WARNs the identical `501`. **A check that degrades to "not testable" on a CONFORMANT peer posture WARNs; SKIP is for "setup could not run" — and it matches the verdict of the sibling check probing the same op.** Both are re-applications (the every-surface/census rule on the op axis; "a skip counts as a failure," ADR-0012) — **no new discipline minted**, per the close-out floor; recorded because the coverage-scope shape is the second bite and the enforcement point (probe every op the rule's text spans; WARN a conformant not-implemented) is concrete. Fixed at `<this commit>`: `op_{compare,compatible}_missing_type_404` added, `runMissingType404` WARNs on `501`; 3-way live: go 35P, py 35P, rust 29P/6W/0F/0S. The sibling-report half — my report said "rust not re-measured — re-confirm" and treated rust as nothing-owed — re-applied the standing sibling-negative rule (a negative about a sibling needs an exhaustive named search, not a ledger inheritance): rust DID owe row-4, measured only when I finally drove its peer.

## Code style & conventions

- Idiomatic Go: `sync.RWMutex` + goroutines; sentinel errors + `errors.Is/As`; functional options (`PeerOption`) for builders; `context.Context` on every I/O op; small provider-defined interfaces (`ContentStore`, `LocationIndex`, `Handler`).
- **No backward-compat shims.** Pre-1.0 takes clean breaks: remove conventions not in the current spec; convert removed-convention asserts into negative/invariant tests rather than leaving them green.

## Project structure

- **`core/`** — protocol library; **13 packages in a strict DAG** (no import cycles): `errors → ecf → hash → entity, crypto, store, types, wire → capability → handler → protocol, tree → peer`. **A package may only import packages to its left.** (`ls core/` shows 14 dirs — the fourteenth is `internal/testutil`, a test helper outside the DAG. This line said "14 packages" until 2026-08-12 while drawing a 13-package chain beside it.)
- **`ext/`** — system extensions, each depending only on `core`. **28 packages** — `ls ext/` is the source of truth, not this list, but the set is large enough that "is there an `ext/X`?" should be answered by looking, never from memory:
  `attestation`, `capability`, `clock`, `compute`, `conformance`, `content`, `continuation`, `discovery`, `encryption`, `handlers`, `history`, `httplive`, `identity`, `inbox`, `localfiles`, `network`, `publishedroot`, `query`, `quorum`, `registry`, `relay`, `revision`, `role`, `signaling`, `storagesubstitutehttp`, `storagesubstitutesources`, `subscription`, `type` (+ `type/constraint`).
  > This line read `clock, inbox, continuation, subscription, revision, role, type, localfiles` until 2026-08-11 — **eight of the twenty-eight.** It understated our own tree by twenty packages, including `identity`, and is exactly the shape that produces a confident false negative about what this repo has built. Re-check it when adding a package.
- **`cmd/`** — CLIs / validation / interop (full inventory in `cmd/README.md`): `entity-peer`, `validate-peer`, `peer-manager`, `entity-sync`, `probe-peer`, `compare-types`, plus `cmd/internal/` (`validate`, `interop`, `config` — not importable outside `cmd`).
- **`docs/`** — `architecture/`, `validation/` (conformance reports, spec-issues), `reviews/`, `legacy/`.
- **Module direction: `core ← ext ← cmd`.** Core never imports ext or cmd.

## Versioning — our number is ours ([ADR-0002])

**Canonical statement + the reasoning: `CHANGELOG.md` §Versioning.** Enforced by
`cmd/internal/hygiene/version_test.go`. The short form:

- **`0.y.z` is this repo's own 3-field SemVer**, forward-only from the published
  `v0.8.0`. **`v7.NN` is the protocol revision we implement** and rides out-of-band.
  **`N · 0F · 0S · 0W @ <commit>`** is what we measured. Three numbers, three jobs —
  ADR-0002 separated them on purpose, and conflating them is the failure it names.
- **Never adopt a sibling's release number.** `entity-core-protocol`'s `0.8.x` is the
  *spec repo's* version. The fleet shares a cut **date**, not a **number**, and the
  numbers are expected to diverge. This is not theoretical: on 2026-08-23 a
  coordination pass wrote `## [0.8.2]` — the protocol's number — into our CHANGELOG,
  and nothing in the tree contradicted it because nothing in the tree had ever said
  what our number was. **A convention that is not written down gets decided by
  whoever asks last.**
- **A cut is ONE edit across every declaration site** — the CHANGELOG heading,
  `ext/go.mod`, `cmd/go.mod`, README §Module paths. The trap is go-specific and
  silent: a sibling-path `replace` hides a stale `require` from *us* and from nobody
  else, because a consumer resolving `.../cmd@vX` gets the requires **without** the
  replaces. A `cmd` cut that still requires the previous `core` publishes a build
  reproducible only inside this workspace. `TestModuleRequiresMatchTheLatestRelease`
  is the gate; it goes RED the moment a release heading lands unaccompanied.
- **The next cut is `0.9.0`** (breaking pre-1.0 changes → MINOR: the §5.4 matcher, the
  `assoc`/`fold` corner rulings, the widened default self-grant, `ext/` 8 → 28).

## Citations on the PUBLISHED surface: name the finding, not the hash

**Earned 2026-08-23, browser-rust caught it downstream.** `core/peer/peer.go` and
`core/peer/handler_grant_ceiling_test.go` both cited `entity-core-rust`'s fix as
`c484fa6` — a commit that exists **on no branch, in no repo**. The rust seat had
committed, reset `dev` locally, and re-committed, twice; `c484fa6` survives only in
that machine's reflog. Nothing was lost (the trees are byte-identical to the surviving
commits and the remote only ever moved forward) but the hash a reader looks up resolves
nowhere. Both files are `.go`, so both **ship**.

The rule is [RUNBOOK-RELEASE-CURATION] + [ADR-0012] Amendment 1 and it is stronger than
the orphan case: `--series` **re-authors every commit** on the release branch, so an
internal SHA quoted in a file that publishes can never resolve for a public reader *even
when it is perfectly alive here*. A sibling's SHA is worse — the reader has no such repo.
**In anything that ships, cite the SYMBOL and the finding** (`their
default_handler_self_grant`, `core/capability/src/lib.rs`) — that is what
AGENTS-STANDARD's *"pin citations to `(symbol, path, commit)`"* reduces to when the
commit half cannot travel. A released version or a tag survives a re-author; a dev SHA
does not.

**Two kinds of SHA live on our published surface and they are NOT the same** — the
distinction is what to check before touching one:

- **A POINTER** — "fixed at `abc1234`", "landed at rust `3e9c9fc`" — invites the reader
  to go look. On the published surface it is broken by construction. Rewrite it to name
  the symbol/behavior; keep a hash only if it is a *tag* or a released version.
- **A RECEIPT** — `cmd/peer-manager/start.go`'s `siblingPinRust` / `siblingPinPython`,
  the corpus artifact hashes, `docs/STATUS.md`'s `@ <commit>` measurement pins. These
  do not ask to be resolved; they record *the state a claim was verified against*, and
  ADR-0012 **requires** them ("a number is only true of the commit it was taken at").
  Leave them. Stripping a receipt would delete the only thing that dates the claim.

**Enforcement, before you write one:** ask whether a reader of the published mirror is
expected to *resolve* it. If yes, it is a pointer and the hash is the wrong citation. If
it is a receipt, say so at the site (`[historical]`, `read live <date>`, `measured @`) so
the next sweep does not "fix" it. The measured scope on the published tree today is ~161
distinct hashes across ~115 files, overwhelmingly receipts; the two above were the only
pointers to a commit that resolves nowhere at all.

## Boundaries — do NOT modify

- **Cross-repo git:** git in `entity-core-go` is in scope — commit/push freely, no per-commit asking (don't force-push or rewrite published history). **Never run git in `entity-core-py`, `entity-core-rust`, `entity-core-protocol`, or `entity-system-architecture`** — those are coordinated through the operator.
- **Spec is the upstream source of truth.** Don't edit `../entity-core-protocol/...` or `../entity-system-architecture/...` from here, and don't write implementation reviews/reports there — those go in `entity-core-go/docs/`. A handed `PROPOSAL-*.md` is **already merged** on the arch side: do the Go impl, don't ask whether to also update the spec or whether to proceed. Spec ambiguities and corrections **to** a proposal go in `docs/validation/spec-issues/` here, and get routed.
- **`EXTENSION-DURABILITY`** is **exploratory / optional / not actively developed** — implemented only as a reference; absence of the surface is conformant; no deployment depends on it (the `durability` validation category reflects this).

## Protocol / interop invariants agents repeatedly get wrong

These broke Python/Rust interop before. Treat them as load-bearing — a regression here is a silent hash mismatch, not a test you'll notice:

- **Hash wire format = a 33-byte CBOR byte string** (`algorithm || digest`), **NOT a CBOR map.** The `Hash` type carries custom CBOR marshal/unmarshal. In-memory it's `Algorithm byte` + `Digest [32]byte` (comparable → usable as a map key); the algorithm byte stays with the hash everywhere, validated once on receipt.
- **Entity `data` stays `cbor.RawMessage`.** Never decode it into a Go map and re-encode — the roundtrip loses byte fidelity → hash mismatch. Byte fidelity is the whole point.
  > **Broadened 2026-08-20 (rust caught it): the no-decode-then-re-encode rule governs ANY byte-identity DECISION, not just entity-data hashing — and a spec's word `byte-identical` is the tell.** The §4.3 pin-delta compares the submitted `pinned_bindings` against the stored ones; my first cut re-encoded the *decoded* `[]PinnedEntry`, which drops a §4.2 forward-compat key a pin may carry — so a config that adds or strips that key read as "no change" and rewrote the registry's **most privileged row** (a pin bypasses the §4.1 step-2 filter and the §6a.9.1 ceiling) under `registry-configure` alone. **Fail-open, on the exact row the check exists to protect.** Same root cause as the entity-data rule, one field deeper: a decode-then-re-encode is lossy on unmodelled keys, and a byte-identity comparison is exactly where that loss becomes a security verdict rather than a hash mismatch. Enforcement: when a spec MUST turns on `byte-identical` (or you are comparing two encodings of the "same" value for an authz/dedup/change decision), compare the **raw field bytes** (`cbor.RawMessage` extracted from the stored entity's `Data`), never a `Decode → re-Encode` of the typed struct. `git grep` for `ecf.Encode(` on a decoded value feeding a comparison. Teeth: `ext/registry` `TestPinDeltaComparesRawBytesNotDecoded` (mutation-verified). The Go-on-Go unit suite could not have witnessed this — go's own codec never emits the unmodelled key, so only a hand-built forward-compat fixture (or a cross-impl peer) bites; **rust reading `byte-identical` literally is what found it**, the same "cross-impl FAIL is the true signal" shape as the invariant-pointer path.
- **ECF must use `CoreDetEncOptions()`.** Any non-deterministic CBOR output breaks hashes.
- **Hash input is `{type, data}` ONLY** — never URI, never content_hash.
- **All paths are absolute, `/{peer_id}/rest`.** Detection is `HasPrefix("/")` (absolute → pass through), not a heuristic; qualify peer-relative paths via `QualifyPath`, and `CleanPath` after any prefix concatenation. **Build URIs with `entity.PathToURI()`** (strips the leading `/` to avoid triple-slash). Reserved `./` / `../` / bare `*/rest` are rejected; there is no `self` token. Path bugs here are coding-discipline failures, not spec gaps — don't file a spec ambiguity for a self-inflicted one.
- **Invariant-pointer paths use 66-char hex with the format byte included** (`00…` for ECFv1-SHA-256 — lowercase hex, format code included; ENTITY-CORE-PROTOCOL "Hex encoding convention" under the invariant-pointer pattern, **normative for chain-participating capabilities** — a cap that travels in a transported/re-verified authority chain MUST bind its signature at the invariant-pointer path or it's untransportable cross-peer), NOT the 64-char digest-only form — every invariant-pointer path (signature / namespace / descriptor) MUST go through `h.Bytes()`, not `EffectiveDigest()`. Watch the trap: if Go's writer and reader share the same wrong form, Go-on-Go PASSes deceptively — the **cross-impl FAIL is the true signal**, so before routing a path-divergence as a sibling's bug, grep Go's own `Bytes()`/`EffectiveDigest()` uses for internal consistency.
- **A self-authored proof of a deliverable a DIFFERENT impl consumes must exercise the CONSUMER's actual dereference, not a producer-convenient path that shares the producer's blind spot (earned 2026-08-20, browser-rust caught it — the C-7 B4 false-green).** I stood up go's federation publisher leg and "proved it end-to-end" by driving go's OWN consumer at it cross-container — and reported it done. The publisher signed a `root_hash` it did not serve (a hardcoded literal, not the served trie root); browser-rust's consumer fetches the signed root **by its content hash** as its first step and 404'd immediately, while my go proof passed because it verified the signature *value* over the hash and walked leaves by *path* — it **never dereferenced the signed root by its hash**, the exact operation whose failure IS the defect. This is "Go-on-Go passes deceptively, the cross-impl FAIL is the true signal" on a new axis: not a shared writer/reader helper, but a **shared verification harness that inherits the producer's leniency**. A go consumer built beside a go publisher tests what go emits, not what the contract requires; the one dereference that distinguishes a served root from a signed-but-absent one is the one a producer-convenient walk skips. **Enforcement: a proof that a producer-side artifact is CONSUMABLE must (1) perform the consumer's real entry dereference — for a published root, `GET /content/{hex(rootHash.Bytes())}` must resolve — and (2) carry a negative control (a wrong/old hash must 404), so the check is shown to discriminate; and a go-on-go pass is NEVER the gate for a cross-impl deliverable — the cross-impl drive is.** Teeth: `scripts/federation-publish.sh probe` fetches the signed root by hash with the old fake hash as the negative control; `cmd/publish-fixture` now publishes `tree.BuildTrieForPrefix(...)` (the real served root), not a literal.
  > **Sharpened 2026-08-20 (B4 half-2, browser-rust caught it AGAIN — the same rule one hop deeper).** Half-1's enforcement — "`GET /content/{hex(rootHash.Bytes())}` must resolve" — was satisfied while the served root was the canonical EMPTY node: `cmd/publish-fixture` wrote entries through `nli` (NamespacedIndex, keys `/{peer}/system/...`) but built the trie through the raw `li`, so `li.List("system/")` matched nothing → `BuildTrie(cs, nil)` → empty root, no error. The signed root verified AND served by hash (half-1 green), and still resolved zero keys. It stayed invisible to go-self because `cmd/fetch-published-fixture` resolves leaves via `FetchTreeLeafPointer` — a **path-index** lookup served straight from `li`, which never walks the signed root trie — so go's own consumer's terminal operation was never the root walk. Two enforcement gaps this exposes: **(1) the consumer-dereference the proof must perform is resolving a KEY THROUGH the signed root, not merely fetching the root by hash** — a root that serves-by-hash but resolves zero keys passes "the root resolves" and fails the contract; the dereference under test is the trie walk to a leaf (`tree.CollectAllBindings(cs, root, "")` in-process, `GET`-walk CHAMP-by-hash on the wire), with a negative control that a wrong/empty root resolves 0 keys. **(2) A writer and reader of one served structure MUST share one key space** — `nli` (writer) vs `li` (reader) disagreed silently and `BuildTrieForPrefix`'s list-with-relative-prefix / trim-with-qualified-prefix shape returns empty rather than erroring exactly when they disagree, so `git grep` the publisher for a `.List(`/`BuildTrieForPrefix` whose index arg differs from the index the entries were `.Set` through. Teeth: `cmd/publish-fixture/main_test.go` `TestPublishedRootResolvesEveryKey` (RED with `li`, GREEN with `nli`) + `TestPublishedRootWalkDiscriminates` (bogus + empty root → 0 keys). **No new discipline earned** — this is the half-1 rule with its dereference-target corrected from "root fetches" to "a key resolves through the root," recorded per the close-out floor; the go-self walk is a REGRESSION guard, the cross-impl drive is still the gate.
- **§4.4 `default_connection_grants` are load-bearing.** When a handler is advertised in the default grants and the spec says peers "depend on" it, the disposition is **implement the handler** — never drop the advertised grant. Spec is contract, not aspiration.
- **An entity resolved from a wire-supplied `included` map BY HASH for an authority decision MUST have its map key bound to its recomputed content hash at the trust boundary — self-consistency (`Validate`) is NOT that binding, and skipping it is a capability/IDENTITY FORGERY, not a robustness nit (candidate, first bite 2026-09-13, core-rust routed it and go confirmed it LIVE by drive).** The `included` map is keyed by wire hashes; `VerifyRequest` (author), `VerifyChain` (root granter / per-link signer / grantee), and `CheckCreatorAuthority` (writer) all read `included[h]` and trust that the entity there IS `h`'s. It is not: `Entity.Validate()` recomputes `hash(type,data)` vs the entity's OWN `content_hash` field (self-consistency), which a substitute passes — it hashes to its own content and is wrong ONLY under the KEY it was filed at; and `content_hash` is a `cbor:"content_hash"` WIRE field, so it is attacker-controllable too. An attacker who knows a victim's identity hash (the **public grantee field** of any cap the victim presents) files their own `system/peer` under the victim's hash; the peer then verifies the attacker's own signature against the attacker's own key while attributing it to the victim — full impersonation, wielding the victim's own cap, no victim key needed (`VerifyRequest` returned `nil` pre-fix — DRIVEN in `core/protocol/included_key_binding_test.go`, not reasoned; rust asked go to drive, per "the cross-impl signal is the true signal / drive don't reason"). **Enforcement: `git grep 'Included\[' core/ ext/` and every `resolve(`/`IncludedResolver` over a wire map that feeds a signature verification or identity resolution — the map MUST pass `entity.VerifyIncludedKeyBinding` (each entry self-consistent AND `key == ContentHash`) BEFORE any such lookup.** Bind it at the boundary, NOT per-call-site (per-site is the fail-open matrix the H1 sweep taught): the receive path (`Envelope.ValidateAll`), `VerifyChain` (the §7a.2a presented-authority arm calls it directly with a merged bundle and relaxes root-trust — bites hardest there), and `VerifyRequest` (author fires before the chain walk; auth-class code because an author-identity forgery is an authentication failure). Teeth mutation-verified PER SITE (each guard neutered reddens exactly its probe), each with a correctly-keyed positive control so the refusal is attributable to the substitution. Fixed `3067ec1`; cohort — rust `0fc01ad`, go here, py unchecked (routed). `verifyRootGranter` was already safe (it re-derives peer_id from the resolved entity and compares to the trusted `localPeerID` — a substitute derives a different id) — which is exactly why the vuln hid: the ONE site that compares against a trusted constant looked like the pattern, and the sites that verify a signature against a resolved-by-hash identity (no trusted-constant comparison) are where it bites.
- **When a presented/delegated capability must satisfy "granter = X" (PD-2 §5.2, and any presented-authority arm), X is the chain ROOT granter — enforce it via `VerifyChain(cap, included, X)`, NOT a leaf-granter equality check — because a re-attenuated credential's leaf granter is the DELEGATOR, not the minter (candidate, first bite 2026-09-09, the release gate's `rexec_delivered` caught it; go unit suite stayed green).** PD-2's presented arm admits "a capability minted by the target." The single-link reentry cap (root = leaf = target) passes a leaf-granter check, so a leaf check LOOKS right — but a continuation `dispatch_capability` (EXTENSION-CONTINUATION §4.2 case 3) is rooted at the target and re-attenuated down to a leaf granted to this peer, so its leaf granter is the installer, and a leaf-granter==target check REFUSES it → falls to ambient → the whole cross-peer continuation dispatch dies. `VerifyChain` with the target as the local reference already enforces root-granter==target (verifyRootGranter) AND rejects the confused-deputy caller_capability (root ≠ target), so it is the ONE check; the leaf granter is then used only for PR-8 resource canonicalization in the coverage step, and the target is the peers/extract_peer reference. **This bit exactly the way this repo's standing "Go-on-Go passes deceptively; the wire/gate drive is the true signal" predicts:** the full go unit suite was green (no unit exercises a cross-peer re-attenuated presented cap), and only `validate-complete.sh`'s `convergence.rexec_delivered` (A hosts a continuation, sub-dispatches tree:get to B) went red — so **run the gate to a true PASS 0..N exit 0 before believing a new authz gate is correct; a green `go test` cannot see a re-attenuated cross-peer presented cap.** Re-application of the wire-drive rule (no new discipline there); the earned, concrete point is the root-not-leaf granter reading with its enforcement (`VerifyChain(cap, included, target)`), teeth in `core/protocol/outbound_authz.go` + the two-arm `origination` check. Owed onward: the 3-way drive vs rust/py once they land PD-2 (the presented-arm root/leaf reading is a likely cross-impl divergence point — measure it, do not assume convergence).
  > **RATIFIED 2026-09-10 — a new authorization ARM must be tested against the base gate it could BYPASS, or the cohort ships the bypass: F67, keystone caught it, and go+rust+py all had it.** PD-2's 0.8.2.17 presented arm said a target-minted credential's *"own four dimensions authorize the sub-dispatch"* while exempting the handler's `peers` scope by name. go implemented `presentedAuthorizes` as a STANDALONE authorizer — a valid presented cap `return`s authorized at `core/protocol/outbound_authz.go:46-50` BEFORE the ambient handler-grant check (`:59-72`) ever runs — i.e. the credential became the whole gate. That is the confused deputy with the ceiling removed: the credential arrives as a caller-supplied param (the scaffold's `dispatch-outbound`), so a caller holding any `T→P` cap steers ANY handler on P past its own grant. **All three ground-up impls implemented the identical bypass** (rust `connection.rs:3545-3557`, py `peer.py:3635-3650`), two with design prose defending it — a spec-licensed defect the whole cohort took. `entity-core-keystone` caught it by reconciling the AUTHORITY MODEL (§3.6b Level-1/Level-2), not the arm's prose; 0.8.2.19 (`4242b18`, E1) fixed it: **the handler grant is the gate on all four dimensions (§6.8); a valid target-minted credential relaxes Dimension 4 (peers) ONLY.** The D1 root-granter reading in the note above STANDS; what was wrong is `presentedAuthorizes` authorizing *alone* — "the leaf granter is then used only for PR-8 canonicalization in the coverage step" described the bypass, now corrected. **Three enforcement points, each a discipline we already held, unapplied: (1)** when you add an authorization ARM (a second way to authorize a dispatch), the mandatory teeth are the COMPOSE-vs-BYPASS discriminator — a case where the new arm's credential is VALID but the base gate (the handler grant) does NOT cover; assert REFUSE (compose), because the bypass form authorizes it. go's probe and unit test had only {credential+covering-grant → succeed} and {no-credential → refuse}, the exact two vectors that cannot see a bypass (arch's F63; and verbatim our own "both arms or it does not discriminate" / "the arm you do not exercise ships wrong" / "carry the teeth" family, now on the multi-authority-composition axis). **(2)** a two-half positive MUST ("the decision is made ON X, never on Y") is not satisfied by answering the negative half — §6.8's "made on the executing handler's grant" is unconditional; go answered only "a credential isn't the propagated caller_cap" and never gated on the handler grant. When a ruling touches a § we implement an adjacent arm against (we implement the ambient arm against §6.8), OPEN that § and reconcile — over-broad arm prose ("its own four dimensions authorize") reads as licensing, which is a spec-issue to ROUTE, not implement from. **(3)** N-way convergence on an authorization arm is NOT validation when the shared reading came from one spec sentence — three impls agreeing on an over-broad clause is cohort-consistency (ADR-0012), and here it MASKED the hole; report `2026-09-09-e`'s "both arms converged" was convergence on the bypass. Do not claim a PD-2 arm converged without the confused-deputy discriminator in the drive. Sweep-method corollary (arch's L23 self-diagnosis, adopt it): a pre-fold/impl sweep keyed on the rule's VOCABULARY (`PD-2`, `presented authority`, `sub-dispatch`) misses homes that state the rule in other words (§6.8 "the executing handler's grant"); paraphrase the SUBJECT with none of its own vocabulary ("what decides whether an outbound sub-dispatch is allowed") and search THAT — if you can't write the paraphrase you haven't found the subject. Owed: the E1 rewrite + the discriminator vector + the 3-way re-drive (`docs/reviews/2026-09-10-pd2-confused-deputy-cohort-wide-audit.md`, handoff `HANDOFF-2026-09-10-pd2-0.8.2.19-and-the-owed-fixes.md`).
  > **APPLIED 2026-09-10 (b) — E1 + the unit discriminator landed, and building it earned one sharper enforcement point: a negative SECURITY test can PASS for the wrong reason, so the mutation against the bypass form — not the observation that it refuses — is what proves the discriminator reaches the gate.** The E1 rewrite (`core/protocol/outbound_authz.go` — `presentedAuthorizes` no longer returns authorized; it feeds a `relaxPeers` bit into `capability.CheckPermissionRelaxPeers`, which gates Dims 1-3 on the handler grant unconditionally and relaxes Dim 4 iff a valid target-minted credential covers the target) is structural: there is no code path for the credential to authorize alone, so reintroducing the bypass requires a NEW early return, which the discriminator reddens. The confused-deputy discriminator FIRST passed with a 403 that never reached the outbound gate at all — the relay grant was unsigned, so `VerifyHandlerGrant` refused the handler at grant-load (once `LocalIdentityHash` was wired) BEFORE the sub-dispatch. "Refused" is the expected outcome of a negative authz test, so an *upstream* refusal is indistinguishable from the refusal under test — the exact shape by which F67's blind vectors passed. **Enforcement (a sharpening of carry-the-teeth for negative authz tests, already covered by the compose-vs-bypass rule above; recorded because it bit while applying that very rule): a negative authorization test is not done when it refuses — it is done when the mutation that REMOVES the protection reddens it. Mutation-verify in both directions (restore-the-bypass reddens the confused-deputy test; neuter-the-relaxation reddens the peers-relaxation control), so the two are pinned in opposite directions and neither can pass vacuously.** No novel discipline (the compose-vs-bypass discriminator is enforcement point (1) above; this is its per-negative-test corollary). The general oracle-class defense — *an authorization arm is not conformance-testable until its bypass discriminator is in the oracle; a gate with N authority sources needs N such vectors* — is written up in `docs/reviews/2026-09-10-b-pd2-e1-implementation-and-the-confused-deputy-oracle-class.md` §3. Still owed: the WIRE discriminator (needs arch's `dispatch-outbound` scaffold contract so all three oracles carry one identical vector — session-b review §4), E4, E3, and the 3-way drive with the discriminator in the set.
  > **APPLIED 2026-09-10 (c) — E3 + E4 landed to cohort parity, and E3 was a LIVE over-acceptance go's own E1 close-out walked past: the enforcement is enumerate the ROOT-GRANTER SHAPES a target-minted relaxation accepts, because the shape the discriminator does not exercise is the one that over-accepts.** go's E1 discriminator exercised a *single-sig* target-minted credential (confused-deputy + relaxation control) but NOT a multi-sig-rooted one — and `presentedAuthorizes` called `VerifyChain(cap, included, targetPeer)`, so a K-of-N multi-sig ROOT that merely INCLUDES the target as one signer landed in `verifyRootGranter`'s M6 "frame peer among signers" branch and relaxed Dimension 4. A K-of-2 credential where the target is one of two signers was accepted as "target-minted." **This was LIVE in go, rust, and py simultaneously** — the cross-impl drive (rust + py) found it; go's own tests were green across it, and go's handoff had framed E3 as *"audit `ResolveGranterPeerID`/`VerifyChain` root handling; confirm fail-closed"* — a verification framing that presumed a no-op. It was a fix. 0.8.2.19 makes it fail-closed: a multi-sig root is not "minted BY the target peer" (D1). Fixed at the GATE, not in M6 (M6 is correct for a peer participating in its own group root; the over-acceptance is only in reading a group root as *target-minted*): `core/protocol/outbound_authz.go` rejects a multi-sig chain root in the presented arm; teeth `TestPD2_E3_MultiSigRootCredentialDoesNotRelax` (a K-of-2 root both-signed so `VerifyChain` accepts it → the `IsMulti` gate refuses; mutation reddens with a 200 — which also proves it reaches the gate, not refused upstream). **Two enforcement points, both re-applications of standing disciplines on a new axis (no novel discipline; recorded per the close-out floor): (1)** the arm-you-do-not-exercise, on the *root-granter-shape* axis — when you test an authority-relaxation predicate, enumerate the root-granter shapes it accepts/rejects (single-sig-target, multi-sig-including-target, multi-sig-excluding-target, foreign-single-sig) and exercise each; the fail-closed obligation differs per shape and the skipped shape over-accepts. **(2)** a deferred delta of a *security* ruling is a presumed defect until DRIVEN — "audit and confirm fail-closed" is the framing that ships the live one; the cross-impl drive is the true signal (verbatim this repo's standing rule, here on go's own deferred delta). E4/F64 (EXECUTE_RESPONSE included-ingestion) landed the same session — enumerated the response reception surfaces (keepalive/wire_event/dispatch-reject consume no `included`; `decodeExecuteResponse` + `connectionDispatcher.Execute` do) and ingested at the two that decode a wire response with auth material; teeth `TestDecodeExecuteResponse_IngestsIncludedSignatures`, mutation `left: None`. Reconciliation + the open arch items: `docs/status/ROUTING-2026-09-10-b-*`.
  > **APPLIED 2026-09-10 (d) — the E1/E3 WIRE discriminators landed (F63 + E3), closing the "owed" wire vectors from (b): an in-process compose-vs-bypass discriminator, even mutation-verified, is NOT cross-impl evidence until the SCAFFOLD grant is shaped so the discriminator is constructible on the wire — a wide scaffold grant makes the compose and bypass readings agree for every input, which is precisely how the F67 bypass passed two green wire rows cohort-wide (no new discipline — this is arch's §7a.1 ⛔ generalization "a gate admitting N authority sources needs N discriminators, and the scaffold must make them constructible," applied and confirmed).** go carried `TestPD2_ConfusedDeputy…` and `TestPD2_E3_MultiSigRootCredentialDoesNotRelax` (both mutation-verified) for the whole arc while the WIRE evidence did not and COULD not exist: the `system/validate/dispatch-outbound` scaffold ran the WIDE default self-grant (`/*/*`, ops `*`), so `dispatch_outbound_reentry` (all-sources-agree→allow) and `dispatch_outbound_ambient_refused` (no-source→refuse) were the only two rows, and both are blind to a bypass. Landed: (1) `ext/conformance/handler.go` `DispatchOutboundHandler.Manifest().InternalScope` is NARROW (echo only, §7a.1 ⛔) — the enabler; teeth `TestDispatchOutboundManifestGrantIsNarrow` (mutation: drop InternalScope → wide default → reddens). (2) `origination.go` `dispatch_outbound_narrow_grant_refuses_out_of_scope` — the F63 wire discriminator, **self-validating**: it drives BOTH arms on ONE target-minted credential — the in-scope op (echo) must return 200 + exactly one reentry (proving the credential is valid+covering, so the out-of-scope refusal is attributable to the narrow grant, not a bad credential — the built-in form of "a negative authz test can pass for the wrong reason", 0.8.2.19 (b)). (3) §7a.1 plural carrier (`reentry_granters`/`reentry_cap_signatures` arrays; all-or-none; partial→400) across the handler + all three oracle producers; teeth `TestDispatchOutboundPartialTripleRejected` + `…AmbientProceeds`. (4) `dispatch_outbound_multisig_root_refused` — the E3 K-of-2 wire vector the singular carrier could not express. **Both wire vectors mutation-verified IN BOTH DIRECTIONS ON THE WIRE, not just in-process** (restore the pre-E1 bypass → only F63 reddens with `BYPASS`; neuter `IsMulti()` → only E3 reddens with `over-acceptance … outer 200 inner 200`); the full gate is a true `PASS 0..4 exit 0` on free ports at the commit, 0 scored FAILs. §7a.2a **ratified shape (a) in-band params** (go's call; cohort already implements it). **Enforcement (re-application, on the wire axis): an authorization discriminator's CONFORMANCE value is zero until it is driven over the wire against a scaffold whose grant is narrow enough for the two readings to diverge — an in-process mutation test proves YOUR gate, never the cohort's; a wide scaffold grant is itself the ⛔ non-conformance, and the wire check FAILs it (message names both causes so a wide-grant peer is not mislabeled a code bypass).** Cross-impl sequencing this creates: rust+py owe the ⛔ narrow grant + plural carrier before the 3-way drive is valid; go built the portable oracle (both vectors run against any `--validate` peer, no oracle change) but has NOT driven them 3-way yet — do not read a green/absent 3-way that does not exist. Reconciliation + §7a.2a ratification + the sequencing: `docs/status/ROUTING-2026-09-10-c-*`.
  > **APPLIED 2026-09-10 (e) — F70: the E3 wire vector was DENY-ONLY and gained its own positive control (arch's §2.4b, keystone's option (b)); no novel discipline — it re-applied carry-the-teeth + mutation-both-directions — but it earned one candidate: a bare `§X` in a check's `Declare` string binds to the NEAREST PRECEDING doc name, so citing a guide section after a protocol citation mints a phantom `ENTITY-CORE-PROTOCOL §X` → `missing-section` → PARTIAL → pass 0b RED (self-inflicted, cost a gate cycle this session).** keystone filed and arch re-verified at the line that `dispatch_outbound_multisig_root_refused` asserted only a refusal (`!(200/200)`, `hits==0`, 403 shape) with its antecedent — *"even though `VerifyChain` accepts the root"* — stated in the declaration and established NOWHERE: if `mintMultiSigReentryCap` produced a credential invalid for any unrelated reason (malformed granter, wrong-bytes signature, grantee mismatch, non-covering resource), every conformant peer refuses it for THAT reason, `hits`=0, outer=403, and the check reports PASS cohort-wide having measured nothing about E3 — a driving-side fault wearing a green three-way. This is §2.4b (a deny-only check MUST establish its own antecedent), §2.4a's mirror: §2.4a scores a correct peer badly (a red gets investigated), §2.4b scores EVERY peer well for the wrong reason (the more impls agree, the more confident the wrong conclusion). Fix per the ruling (option (b), the wire differential; option (a) — an in-process `VerifyChain` assert — accepted as a cheap FLOOR beneath the control, refused AS the control: your construction validated by your verifier catches a typo, not a shared misreading): `SendDispatchOutboundE3MultiSig` now drives TWO presented sends over the same echo op/target/scope through the existing `sendDispatchOutboundPresented` — **single-sig target-minted first as the positive control** (MUST succeed 200/200, reentry `hits`→1), then the K-of-2 multi-sig over the identical request, **granter form the ONLY variable** (`mintReentryCapForOps` and `mintMultiSigReentryCap` are byte-parallel except `Granter`). The check FAILs if the control does not succeed (antecedent not established → the refusal is unattributable), FAILs on over-acceptance (`hits` 1→2, both succeed), carries-the-teeth (`hitsAfterMultiSig` must stay at the control's 1), else PASS on either refusal shape / WARN on an unexpected one. Mutation-verified ON THE WIRE: neuter `IsMulti()` → E3 reddens on the over-acceptance branch *while the single-sig control passes* (so the red is attributable — exactly what §2.4b buys) and **only** E3 reddens (F63 stays green); the mechanical tell arch named — grep declarations for *"even though" / "while" / "despite"* and ask which arm establishes the clause — now finds the E3 row's antecedent established by its own single-sig arm. Enforcement for the candidate: when a `Declare` string cites a guide section by bare `§number`, qualify it with the doc name (`GUIDE-CONFORMANCE §2.4b`) OR state the rule in words without a `§` — never leave a bare `§X` downstream of a different doc's citation, and after touching any `Declare` run `go run ./cmd/conformance-register -check` to a true exit before the gate (the trap is invisible to `go test`/`go vet`; only pass 0b sees it). `origination.go` `dispatch_outbound_multisig_root_refused` + `conformance_handlers.go` `e3Outcome`/`SendDispatchOutboundE3MultiSig`; the in-process `TestPD2_E3_MultiSigRootCredentialDoesNotRelax` stays as the option-(a) floor. Full gate a true `PASS 0..4 exit 0` on free ports @ this commit. Reconciliation to arch (discharges §2 of ROUTING-2026-09-10-c): `docs/status/ROUTING-2026-09-10-d-*`; relay of arch's §4 to rust+py: `docs/status/ROUTING-2026-09-10-e-*`.
  > **AMENDED same session — arch caught that this fix DISCHARGED the §2.4b grep-tell by DELETING the antecedent instead of ADDING the control, and amended §2.4b (arch `2c941a6`): a declaration satisfies §2.4b only if it KEEPS the antecedent AND names the arm that establishes it.** The first cut of the fix rewrote the `Declare` from *"MUST NOT relax Dimension 4, even though `VerifyChain` accepts the root"* to a bare description of the pairing — check conformant, control real — but arch's own mechanical tell (grep declarations for *"even though" / "while" / "despite"*) then no longer found this row, so a seat could clear the tell by deleting the clause rather than building the control. **A rule's first real application is the first measurement of it** (arch's framing): §2.4b as shipped 2026-09-10 was defeatable by its own tell. The amended rule, now in GUIDE-CONFORMANCE and re-applied here: the E3 `Declare` keeps *"even though `VerifyChain` accepts the root"* AND states *"that antecedent … is ESTABLISHED, not merely asserted, by the single-sig target-minted positive control … this row names the arm that does"* — the grep tell finds the clause, and the clause names its establishing arm. Enforcement: when you write or edit a deny-only check's declaration, keep the *"even though / while / despite"* antecedent clause in the prose (do not sanitize it out) AND, in the same declaration, name the positive-control arm that establishes it; a declaration a `git grep 'even though\|while\|despite'` cannot find is not §2.4b-conformant no matter how good the control is. Applied at `<this commit>` (`origination.go` `dispatch_outbound_multisig_root_refused`), no logic change (the check body is unchanged; only the declaration prose), gate re-run to a true `PASS 0..4 exit 0`, register still matches baseline. (arch's second finding — a bare `§X` binding to the wrong DOCUMENT is the sibling of a citation resolving to the wrong SECTION, invisible to every gate one level up — is filed by arch as an arch-tools candidate, not go's to build.)
  > **DISCHARGED + a harness bite, 2026-09-09 (b): the 3-way drive ran (go↔rust↔py), PD-2 converged on the root reading at all three — AND it caught a harness defect that is the "a shared verification harness inherits the PRODUCER's shape" family (B4 federation-publish) on the conformance-CHECK axis.** go's `dispatch_outbound_ambient_refused` check understood only go's OWN scaffold result shape — the ambient Dimension-4 refusal WRAPPED as outer 200 + inner 403 (go, rust) — and wrongly SKIPped py, which RELAYS the same refusal as the outer status (403 capability_denied). A conformant peer scored "unmeasured" because the check read the authoring seat's shape as the only shape. **The rule: a cross-impl conformance CHECK for a scaffold whose result shape the spec does not pin MUST enumerate and score every shape the cohort emits — not only the authoring seat's — or it mis-SKIPs/mis-FAILs a conformant sibling; the cross-impl drive is what exposes it, a go-on-go run never can (it only ever sees go's shape).** Enforcement: `dispatch_outbound_ambient_refused` now passes on outer-403-capability_denied (relayed) OR outer-200+inner-403 (wrapped), and SKIPs only a genuine outer-400 param-validation refusal (`conformance_handlers.go`/`origination.go`, `2243a50`); the unpinned scaffold shape is routed to the cohort (report `2026-09-09-e` §2) to pin one. Re-application of the producer-shape-leniency family + "the cross-impl drive is the true signal" — recorded per the close-out floor; no novel discipline. (The register `ambiguous_resource`-vs-`path_required` split this cycle re-applied D12 "read the explicit spec section, don't follow the sibling's count" — §3237 explicitly pins `ambiguous_resource`, so go kept it and routed the §3237-vs-§3.3 contradiction rather than flipping to py's `path_required`; spec-issue `2026-09-09-b`. Also no novel discipline.)
- **A hash-determining rule we already implement ONCE is the canonical source — read our own tree before reaching for a stdlib or an external standard, and never cite the external one as authority when our own code answers it (ratified 2026-08-18 (g), and it bit for MONTHS).** `ext/revision/globMatch` was written at v0.8.0 on Go's `path.Match` — a **segment-scoped `*` that does not cross `/`** — and then grew a `**` token to recover the crossing behavior. But our canonical path-glob rule is `ENTITY-CORE-PROTOCOL` §5.4 `matches_pattern`, and `core/capability/check.go:MatchesPattern` + `ext/subscription/engine.go:matchPattern` have BOTH used it since v0.8.0: there `*` is a **subtree wildcard that crosses `/`** (`prefix/*` → `HasPrefix`), and `/` is special only in the leading `/*/` peer form. So `system/revision/*` already reaches any depth and the whole `**` apparatus was buying back what §5.4's `*` gave for free. **Two things made it invisible and both are the discipline:** (1) the matchers live in different packages and **nothing compared them**, so a hash-determining concept had two contradictory implementations — `*` crossing `/` in capability/subscription, not crossing in revision — which is a version-hash fork by configuration waiting to happen; (2) when the operator challenged it I argued **bash / gitignore / `path.Match` semantics** instead of opening our own §5.4 matcher, which — once read — showed the operator was right and the token was never ours. **The rule, both halves: (a)** for "what is *our* rule for X," the canonical source is our own spec + the existing implementation of X in this tree, checked FIRST; an external convention informs but never *defines*, and citing it as authority when `git grep` finds our own rule is the anti-pattern (D12, read canonical sources — applied to our own code, not just the spec). **(b)** a hash-determining concept (a glob, an encoder, a canonicalizer, a path resolver) implemented in more than one place MUST either share the implementation or carry a cross-implementation consistency test with a deliberate, named divergence — two silently-diverging implementations of a hash-determining rule is a DAG fork nobody sees until a peer does. Enforcement: the revision matcher is now §5.4's four forms plus one *named* addition (`*<literal>` suffix, §2.4 form 3), `glob_test.go` pins the REV-GLOB-* vectors, and `auto_version.exclude_pattern_four_forms_enforced` measures the write-time `**` rejection three ways on the wire. Before adding a matcher/encoder/canonicalizer, `git grep` for an existing one and diff the semantics; a divergence you did not deliberately choose is a bug you have not noticed yet.
  > **Broadened 2026-08-18 (r), earned by a third bite — the trap is NOT unique to hash-determining rules.** `ext/registry`'s `name_format_dispatch` matcher (§4.1a) was on `path.Match` since v0.8.0 — the *same* `path.Match` stdlib-semantics mismatch as the revision `globMatch` and the §4.6 `type_pattern` matcher — but a dispatch filter is a **resolution-routing** concept, not hash-determining, so (g)'s "hash-determining concept" qualifier would have skipped it in a proactive sweep. It hid until read at the call site. Arch ruled it closed at [REGISTRY 1.13] (`*`-only, every other byte literal incl. `/`, no invalid pattern), landed at `08684b2` as `matchDispatchName` + `REG-DISPATCH-GRAMMAR-1`. **The correction: (g)'s enforcement — `git grep` for an existing matcher and diff the semantics before reaching for a stdlib glob — applies to ANY matcher/predicate for a spec-defined field, hash-determining or not.** That grep is what surfaced the FOURTH matcher, `name_constraints` (§6a.9), still on `path.Match` and its grammar unruled — routed as a deliberate, named divergence (spec-issue `2026-08-18-e`, call site commented), not converged unilaterally, per the divergence rule. **No novel discipline earned** — the route-don't-ship call (arch ruled, then I landed) re-applied the divergence rule; the wire check honestly under-claiming the `/`-crossing row (unbindable per §6.3, so pinned in-tree not on the wire) re-applied ADR-0012 don't-overclaim. The only earned change is dropping "hash-determining" from (g)'s scope.
  > **CLOSED 2026-08-19 (a): `name_constraints` is ruled + landed, and the divergence-route paid off.** Arch ruled it onto §4's ONE matcher at [REGISTRY 1.15] (`c984f93`, `ROUTING-2026-08-19-c`) — confirming spec-issue `2026-08-18-e`'s "most likely" reading — and go landed it at `fbaf838`: `name_constraints` now calls the SAME exported `registry.MatchName` as dispatch (shared, not a second matcher — the one-matcher ruling made D1 of (g)/(r) literal), and the `path.Match err→500` arm is deleted as unreachable. The §4.1 step 2 *filter* was ruled the same round at [1.14] (`86643f8`, `ROUTING-2026-08-19-a`) — confirming spec-issue `2026-08-19-a`'s security argument — and go deleted the `any` guard so an unmatched name narrows to empty → `chain_exhausted` (teeth: `ext/registry/dispatch_filter_test.go`, RED against the reintroduced guard). **The whole worklist re-applied existing disciplines and earned no new one** (per the close-out floor, recorded as such): one-matcher = share-don't-duplicate (g/r); the `/`-row pinned in-tree = §6.3-unbindable (the dispatch-grammar disposition, now also filed as spec-issue `2026-08-19-b` for §11.1 to mark both `/`-rows in-tree-only); the deferred filter wire-vector = the deceptive-green rule below. Cross-impl on the new `name_constraints_grammar` vector: **go + py PASS (32/32), rust FAIL row 1** (`a?c` admits `abc` — rust still `?`-as-wildcard, the divergence 1.15 closes; arch had not measured rust) — `docs/validation/reports/2026-08-19-*`.
- **A `compute/error` has TWO in-language forms — cover both, or you have 0% coverage of one.** A minted error is a `*ComputeError` (a Go error, auto-propagating via `if err != nil`); a value-form error is an `entity.Entity{Type: compute/error}` reached by SA-1 (a literal, or a `compute/lookup/hash` resolving to a stored error) with a nil Go-error. `is_error` (§4.1, kind-based) unifies them, but ONLY where it is applied — so every value-consuming site must go through `evalOperand` (Evaluate + is_error), and every crossing (handler F10, reactive `result_path`, **SA-9 `store`**) must recognize both. **Earned 2026-08-16, measured:** the §2131 short-circuit was missing for the value form at five sites (arithmetic masked it as `type_mismatch`; compare/if/field didn't short-circuit; the reactive crossing wrote it verbatim — a §2.4 divergence). It was invisible because **every error-path test minted its error the same way** (a failing op / an unresolvable-hash lookup → the `*ComputeError` form) — 100% coverage of one representation, 0% of the other. py hit the identical blind spot (~300 tests). **Enforcement: an error-path test asserts BOTH forms** — a failing op AND a `compute/error` literal / a lookup onto a stored error — at each consumer (`TestValueFormErrorShortCircuits`, `TestReactiveErrorCrossingIsCodeOnly`, `TestStoreErrorCrossingIsCodeOnly`). Whenever a value type has two internal representations, a test that exercises one has not touched the other.
  > **Same-session addendum — the crossing list is itself a surface I under-enumerated.** The 877edf5 close-out fixed the `result_path` crossing and named it, but **§2.4 has TWO write-site crossings — `result_path` AND SA-9 `store`** — and the store crossing had the identical gap: the value form hit `materialize()` (which rejects a `compute/error` post-v3.23) → an *internal* error, wrote nothing; the minted form propagated instead of writing. Testing one write site was 0% coverage of the other — the same discipline biting **one level up**, on the *set of surfaces* rather than the *set of forms*. **No new discipline earned** (the existing two-representation rule applied to a surface I'd missed, not a novel invariant — per the close-out floor); the enforcement is completed: **enumerate every §2.4 crossing, not "the crossing."** The check is a grep of the rule's own surfaces — `git grep 'materializes where.*written\|SA-9 .store' ../entity-system-architecture/specs/extensions/EXTENSION-COMPUTE.md` lists both write sites; the test set must cover each. Fixed in `builtinStore` (both forms → `ToMaterializedEntity`, code-only); the write-vs-short-circuit reading it forced is routed (`docs/validation/spec-issues/2026-08-16-b-*`).
  > **Addendum 2026-08-21 (rust + py caught it) — a SHARED consumed-operand RESOLVER using bare `Evaluate` masks the short-circuit for BOTH forms, and a three-way corpus LOCK does not cover it (no new discipline — the existing "every value-consuming site goes through `evalOperand`" rule, with the enforcement sweep it lacked).** `resolveCollection` (the `collection` operand of map/filter/fold/assoc/group-by) called bare `Evaluate` then type-asserted `[]interface{}`, so a value-form `compute/error` operand fell through to `type_mismatch` — the §7.2 consumed-position short-circuit was missing entirely (not a two-representation split; a total miss at that operand). `concat` carried the same slip one level down (its per-sub-collection `c.([]interface{})` type_mismatched an error sub-collection, where §7.2 makes each sub-collection consumed→short-circuit — distinct from an error ELEMENT, which is CONTAINED, CV-5). **The go compute corpus LOCKED 352/352 three-way (go↔rust, go↔py) with the bug LIVE**, because no vector placed an error in the collection operand — py's "the two readings agree everywhere your fixtures live" law, with the corpus AS the fixture set: a LOCK proves agreement only on inputs the corpus contains. Two enforcement points, both grep-able: **(1)** `git grep 'Evaluate(' ext/compute` for any resolved operand that is then **type-asserted** — a consumed operand resolved with bare `Evaluate` instead of `evalOperand` is the shape (index/length already use `evalOperand`; the collection resolver did not); **(2)** the corpus discriminator for a consumed operand is an error IN that operand position — enumerate every consumed operand a primitive has (collection, index, key, each sub-collection) and seed an error-in-each, not only the positions already covered. Fixed: `resolveCollection` → `evalOperand`; `builtinConcat` short-circuits an error sub-collection; teeth `TestCollectionOperandErrorShortCircuits` (5 ops × both forms) + `TestConcatSubCollectionErrorShortCircuits`; corpus CV-7a/b/c (352→355, SHA re-pinned `86bc5e8a`). The go-on-go corpus passing was never the signal — the cross-impl catch was (this repo's standing "Go-on-Go passes deceptively, the cross-impl FAIL is the true signal," here on the axis of *corpus coverage* rather than a shared helper).
  > **Corpus addendum (the three-way run, `docs/validation/reports/2026-08-16-c-*`) — two existing disciplines, two new surfaces, both caught by a control, no new rule minted.** (1) The corpus *harness* had the same two-representation split: `boundaryOf` reduced a value-form `compute/error` result to entity-kind while `evalVector` reduced a minted one to error-kind, so the reduction disagreed with the wire (F10) on exactly the store vectors. **The route-A-vs-route-B control (go in-process vs go driven over its own wire) is what caught it** — run that control before trusting any cross-impl lock; a spurious inproc-vs-wire divergence is a harness-reduction bug, not a peer bug. Fixed: a `compute/error` result reduces to error-kind everywhere. (2) **To gate a property of a WRITTEN entity, read the written entity — not the eval result.** §2.4 code-only is a property of the *stored* bytes; the eval result cannot show it (a `compute/error` result reduces to error-kind, keyed on `code` and blind to `message`, and over the wire F10 re-wraps it with a fresh message so it is not even the stored bytes). The store vectors carry `Vector.BoundaryPath`: the boundary is the content hash of the entity at the store path, read out of band (in-process location index; wire `TreeGet`). This is "conformance-green ≠ correct if the test asserts the wrong thing" (ADR-0012), and the fix is to assert on the right artifact — the written entity — after which the gate went from a false LOCK (codes agreed) to a true RED: go writes code-only, rust/py write nothing (propagate). **Do not vote the correct impl wrong because the cohort agrees** — cross-bless labels the store divergence "go's bug (one-differs)" because rust+py agree, but §2.4 makes go correct; the spec arbitrates (routed: spec-issue `2026-08-16-b`).

- **The wire-entry and in-process-entry dispatch paths populate the handler CONTEXT by different mechanisms, so an equivalence test that asserts only the RESULT misses a field one path drops (earned 2026-08-18, workbench-go caught it with a kernel reproducer).** `handleExecute` (wire) seeds `hctx.Resource = normalizeResourceTargets(...)` **directly** on the handler's context; `DispatchLocalExecute` (the SDK entry, how every in-process consumer dispatches) instead re-dispatches through `makeLocalExecute`, whose child context takes its resource from `execOpts.Resource` — **not** from `rootCtx.Resource`, because that closure is also the sub-dispatch path where §5.2 forbids inheriting the parent's resource. So `DispatchLocalExecute` set `rootCtx.Resource` and it never reached the handler: every in-process ENTRY dispatch that named a resource dropped it to nil, silently, at 200. `dispatch_equivalence_test.go` asserted RESULT-hash equality across the two surfaces and `subdispatch_resource_dimension_test.go` drove both sub-dispatch directions — but nothing entered through `DispatchLocalExecute` with a resource and read it back off the handler's context, so the drop was invisible for as long as it existed. The rule: **when two entry paths claim observable equivalence, the equivalence test asserts the handler-visible CONTEXT (resource, bounds, caller-cap, every dimension the two paths set by different mechanisms), not just the Result** — a field the wire path sets on `hctx` directly and the local path must thread through an `ExecuteOption` is exactly where a newly-added field silently diverges. Enforcement: `TestDispatchLocalExecute_CarriesResourceToHandler` (a handler that records `req.Context.Resource`, driven through the entry point, teeth-verified red pre-fix); when you add a field that the handler context carries, assert it reaches the handler through BOTH entry surfaces. (Fix: `DispatchLocalExecute` passes `handler.WithResource(req.Resource)`, mirroring wire entry.)
- **A restart-durable rollback-defense counter held ONLY in process memory silently rolls back on restart — recover it from the store at startup (candidate, first bite 2026-08-18 (r), workbench-go's cross-impl run).** `ext/publishedroot`'s publisher minted `seq` from an in-memory `lastSeq` that started at 0 every construction, so a process restart republished `seq=1` over a store that already carried `seq=N` — and §6.5.3.1 makes `seq` a **rollback defense**, so every consumer that saw `N` rejects the new head, the defense working as designed against a bug. The tell it was a bug and not a design: **the same concept was already implemented correctly one directory over** — `ResolutionLogData`'s §11.2 seq "is recovered on startup by walking this prefix and taking max+1" (`core/types/registry_ext.go`), and published-root simply didn't do the equivalent. Same meta-shape as the (g) family: one place got a concept right, another held it in memory only, and **nothing compared them**. The rule: **a monotonic / rollback-defense value that is bound in the durable store MUST be recovered from that store before the next mint, not carried in zeroed process memory** — held in memory it is correct until the first restart and then silently wrong, which no same-process test can see. Enforcement: `TestSetupAuthorityRecoversSeqAcrossRestart` drives two publishers over ONE store+index and asserts the second continues the chain (teeth: seq=1 without the recovery); when you add a store-bound counter, add a two-lifetime restart test, and `git grep` for a sibling that already recovers-on-startup before writing a fresh one. (Fix: `SetupAuthority` reads the bound head at `PublishedRootStoragePath()`, seeds `lastSeq`=`pr.Seq` and `lastHash`=the bound hash.)

- **A conformance harness must transmit a resource LIMIT through the SAME spec channel the peer READS it from — a limit sent on the wrong channel does not error, it silently DEFAULTS, so the tell is a boundary that equals the peer default, never a failure (earned 2026-08-22, the compute cross-bless did not lock — same deceptive-green family as "Go-on-Go passes, the cross-impl FAIL is the signal," on the axis of a shared DEFAULT).** The 362-vector compute cross-bless read `361/362` for a week: `cv9a-map-depth-exceeded-contains` (and `recurse/tail-sum`) forked because the wire driver (`cmd/internal/compute-corpus/peeremit.go`) sent only `params.budget` (operations) and EXTENSION-COMPUTE §5.2 sources the DEPTH limit *only* from the capability's grant-level `constraints["system/compute"]["max_compute_depth"]` (ENTITY-CORE-PROTOCOL §5), never from a param. So every depth-sensitive vector ran at the peer default depth (1024) and returned the full array where the in-process eval returned `depth_exceeded`. It was invisible go-on-go (both sides defaulted → agreed) and surfaced only cross-impl. **It compounded with a real go HANDLER bug on the same read:** `initBudget`/`reactiveBudget`/`EvaluateAtPath` read `constraints` off the capability TOKEN's top level (`cap.Data["constraints"]`), but per §5 `constraints` is a GRANT-level field (`grants[].constraints`; the token has no top-level `constraints`), so a compute depth/ops constraint had reached the evaluator *nowhere* since v0.8.0 — the field `HandlerContext.MatchingGrant` ("grant entry that passed CheckPermission (for constraints)") existed for exactly this and was unused. Two enforcement points: **(1)** for any resource limit a harness reproduces on a peer, confirm the *channel* — grep the handler's `init*Budget`/limit-read for what field it consults, and drive the limit through THAT (here: mint a cap whose matching grant carries the constraint via `SendExecuteWithCap`, not a param); the acceptance proof is a route-A-vs-route-B LOCK (go-in-process vs go-over-wire, `cross-bless` 362/362), which is what a silent default cannot pass once the vector is depth-sensitive. **(2)** a handler reading a §5 grant-level field (`constraints`, and its siblings) off the token top level returns nothing for every conformant capability — `git grep` reads of `constraints` in a handler and confirm they resolve the MATCHING GRANT (or scan the token's grant entries), never `token.Data["constraints"]`. Teeth: `ext/compute/depth_constraint_test.go` (grant-level `max_compute_depth` lowers the budget through `initBudget`; mutation = strip the constraint, default stands) + the go-on-go wire LOCK; the §5.2 pseudocode that misled the read (`capability.data.constraints`) is routed as spec-issue `2026-08-22-a` so rust/py don't inherit the same silence — the cross-impl re-drive of the 362 corpus is the confirmation that owes.

### Posture on protocol work (Go leads)

- **A PROPOSAL is the floor for implementation; a FOLD is not — and "implement against the landed spec, wait for the fold" is a leak, not the method.** We are the trailblazers. We implement from a landed proposal or a draft and do **not** wait for arch to fold it into the core spec. Implementing is *how* a proposal gets validated — the wire feedback is what lets arch fold **once**, instead of the merge→amend→re-merge churn that happens when a spec is folded before anyone has built against it. How arch knows what to fold is that we built it first. The bar is: (1) there is a proposal/draft behind the change — do not invent semantics from nothing; (2) the reading has **low degrees of freedom** and the impl cohort basically agrees. Clear the bar → implement now, hand arch the **result** (and the feedback to clean up the fold), do not ask a question. A **genuine** ambiguity — the analysis is open, the cohort splits, a value judgment is in play — is the exception you route (`docs/validation/spec-issues/`), while still implementing the unambiguous rest. This kept re-appearing as "implement against the landed spec, not in-flight proposals" in the shared `AGENTS-STANDARD.md`; that line was corrected in this repo's copy on 2026-09-04 (operator ruling) to "implement against a landed proposal or draft; you do not wait for the fold." **Owed:** the same edit to meta's master + a fleet re-inject (coordinate with DevOps at the next overlay reset) — until then the corrected text holds only here. Never re-derive the old gate.
- **Defer the GATE, never the IMPLEMENTATION.** The wire-conformance-check-deferral discipline (a check must not ride an unruled / cross-impl-divergent discriminator) is about not shipping a *conformance gate* that encodes a contested reading — it is **not** license to withhold the *behavior*. Build the behavior from the proposal now; hold only the specific gate whose PASS/FAIL turns on a genuinely open semantic, and while it is held, cite the **proposal** (not a not-yet-folded or colliding §number) at the impl site.
- **Don't defer on "implementation-defined" or "Rust/Python don't have it yet."** We are the impl team — implementation-defined means *we* define it; multi-Go-peer validation is real validation; cross-impl is downstream feedback.
- **Don't gate a merged spec on "hot path / blast radius."** The hot path is the system. Make the change additive (opt-in, nil-default) so there's no blast radius to agonize over, then implement it. Reserve questions for genuine value judgments; for a real **spec ambiguity**, write it up under `docs/validation/spec-issues/` and keep implementing the unambiguous parts.
- **On a fresh probe that FAILs one cohort sibling on a wire-shape detail:** check whether adjacent categories already tolerate that shape (reuse their extractor, e.g. `extractStatusAndCode`) before calling it a peer bug — a too-strict probe FAILs a tolerated divergence. Flag the divergence in writing; don't gate the cycle on it. (This is *not* "permissive forever," and never relabel a real FAIL "pre-existing" without bisecting.)
- **A wire conformance check MUST NOT discriminate on a value that depends on an UNRULED / cross-impl-divergent spec semantic — such a check tests the contested reading, not the feature; it passes only against the impl that shares its reading; and its green is a false signal that ONLY a cross-impl run exposes (ratified 2026-08-18/19, rust caught it — a second bite of "Go-on-Go passes deceptively, the cross-impl FAIL is the true signal," on a new axis).** `registry.v15_dispatch_grammar` (08684b2) tried to observe the §4.1a *matcher grammar* by watching `resolved` vs `chain_exhausted` — but "does an UNMATCHED name still resolve?" **is** the §4.1 step 2 *filter* question, which is unruled and go/rust read opposite (go: unmatched → unfiltered → resolved; rust: unmatched → scoped away → chain_exhausted). So the check measured the filter, encoded go's reading, passed go-on-go, and FAILed a conformant rust peer — a false conformance signal (ADR-0012). Worse, the negative-match row (the only one that distinguishes our grammar from `path.Match`) IS inherently the contested filter case, so **no honest wire check exists here until §4.1 is ruled** — the grammar's teeth belong in-tree (`glob_dispatch_test.go`), where they are uncontested. The published-root path fix this same session was the *sibling* bite (shared writer/reader helper → Go-on-Go green → Rust consumer caught the EISDIR), so this pattern bit **twice this session** and is ratified. **Enforcement: before landing a wire check, name the exact observable its PASS/FAIL turns on, and confirm every conformant impl agrees on that observable under the SETTLED spec; if the discriminator rides an open spec-issue (or a `git grep ../entity-core-*` shows a sibling reading it differently), pin the feature in-tree and DEFER the wire check with a comment naming the spec-issue.** A green wire check that can only be green on the impl that wrote it is not evidence — it is the deceptive-green trap wearing a conformance badge. (Removed; §4.1 step 2 routed as spec-issue `2026-08-19-a`, concurring with rust.)
  > **Forward half, 2026-08-19 (a) — a check deferred for an UNRULED discriminator is a REVISIT obligation on the ruling, not a permanent grave; and the same check text flips from deceptive-green to true-signal the moment the semantic settles AND every conformant sibling is verified to agree.** §4.1 step 2 was ruled at [1.14] (`86643f8`) — the filter discriminator is now settled — so the deferral reason changed from *semantic* (unruled) to *sequencing* (arch `ROUTING-2026-08-19-a` §4: constructible only once rust + py also land row 2, else it FAILs a not-yet-landed conformant peer). The v15 filter vector therefore STAYS pulled, now for the sequencing reason, and the revisit is recorded in the check's own comment (`cmd/internal/validate/registry.go`). **The sibling grammar check went the other way and proves the rule's teeth:** `name_constraints`'s grammar was *also* unruled and its wire vector was withheld — but 1.15 SETTLED it, so `name_constraints_grammar` became constructible, and a cross-impl run (not go-on-go) confirmed it: **go + py PASS, rust FAIL** on a settled semantic. A FAIL against a settled-and-cross-impl-verified discriminator is a TRUE conformance signal (rust owes the landing); a FAIL against an unsettled one is the deceptive trap. The enforcement gains one clause: **when the spec-issue a deferred check named is CLOSED, re-evaluate the check — (a) is the discriminator now settled? (b) does a cross-impl run show every conformant impl agreeing? Only then land it; a green-on-author-only recheck means it is still riding contested ground.** No new discipline (the forward reading of the same rule); recorded because the revisit is the step a "deferred pending ruling" note otherwise silently drops.
  > **DISCHARGED 2026-08-19 (b): the filter vector's revisit fired and it landed as `v4b_meta_resolver_filter_fail_closed`.** The two-clause test above was run, not asserted: (a) the §4.1 step 2 discriminator is settled ([1.14], `86643f8`); (b) a cross-impl run — not go-on-go — shows every conformant impl agreeing: **go + rust + py each PASS registry 15/15**, the vector green on three independent codebases (peers built fresh at rust `898e55b`, py `1cb7c01`, the row-2 landings the sequencing gate named). The construction is the fail-closed teeth: a name that is BOUND (so the chain WOULD resolve it) but matches no dispatch rule must narrow to `chain_exhausted`, with a positive control (catch-all rule → the same name resolves) that SKIPs rather than PASSes if the setup can't reach resolution — the wire form of ADR-0012's carry-the-teeth. Release gate `validate-complete.sh` exit 0, all six passes. **No new discipline earned** (per the close-out floor): v4b re-applied the two recorded rules verbatim — the settled-discriminator-plus-cross-impl-agreement gate (this block) and carry-the-teeth (ADR-0012) — and the deferral comment in `registry.go` is rewritten from "held, sequencing" to "landed, cross-impl green." The MATCHER grammar (1.15) stays in-tree: its only distinguishing row is `/`-crossing, unbindable per §6.3, so unobservable on the wire (spec-issue `2026-08-19-b`). The point worth carrying: a "revisit obligation" is only discharged when someone runs the two-clause test and lands or re-defers — a note that stays "STAYS pulled" forever is the same silent drop this rule was written against.

## Verify build state before you assert it — READ THE LIVE WORKTREE

**"Filed, not yours" is about provenance, not ownership — a normative change to a definition site we
bind against is owed work no matter which section announces it (earned 2026-08-18 (m), arch caught it).**
We acknowledged arch's `-i` ask to confirm "any doublestar dependency removed" and it was **false**:
go carried the §4.6 `type_pattern` doublestar matcher (`ext/type/constraint/globMatch`) the whole time,
and we confirmed "removed" without opening it — because `-i` filed the §4.6 fold under a heading
("Arch-owed, filed, not yours") that told three engine seats the change was not theirs. It was *their
defect to fix* (provenance) but a definition site *we implement a matcher against* (ownership of the
follow-on), and all three of us confirmed removal of a dependency we all still had. Our own spec-issue
then reasoned from the pre-fold §4.6 text, two commits stale. **The rule: when a fold/ruling touches
spec text this repo implements a matcher / encoder / validator / canonicalizer against, open THAT
surface and re-read our code before acknowledging anything about it — regardless of whether the packet
files it as ours.** Enforcement: an acknowledgement of "removed / changed / conformant on X" names the
`(file, symbol)` it checked (here `ext/type/constraint/handler.go` `globMatch`), never a bare "confirmed";
a §-heading that says "not yours" is not evidence about our tree, only about whose defect it was. Same
family as the sibling-negative miss — the sentence nobody opens the tree for is the one that ships wrong.

Any claim about what another repo *has built* — a flag it honors, a surface it lacks, a seam that
is unbuilt — is a **build-state claim**, and it decays the moment that repo commits. Before making
one:

- **Open the tree.** `git status` + HEAD + **read the actual source**. A report, a handoff, or
  `git log` alone is not sufficient; run `git log --since` on every sibling the claim touches.
- **Cite `(repo, commit, date)` at the point of the claim**, not in a preamble.
- **Never carry a dated measurement across a commit change.** If the target moved, re-measure or
  re-pin — a number is only true of the commit it was taken at (ADR-0012).
- **A peer's correction outranks our inference, immediately.** They can read their own tree.

**The shape, so it is recognizable:** a stale build-state claim is always *plausible* — it was
true when written and reads fine forever after. Review does not catch it; only opening the tree
does. It has been caught four times, every time by a peer, never by us re-reading our own text.

**It fails in both directions.** A capability note can be wrong by being **ahead** of reality as
easily as behind it — `--publish-root` once asserted republish-on-change as though the spec
required it, when no landed spec obliges a publisher to republish at all. "Rust + Python pending"
outlived the gap it described; "honored by all three" preceded the rule it implied. Both are the
same defect, and both read as a sibling bug that isn't there.

This applies hardest to **`cmd/peer-manager` flag help**, which is where these claims accumulate:
every `Honored by …` / `… impl pending` string is a build-state assertion about a repo we do not
control. Re-verify them whenever a sibling moves, and treat a stale one as a defect, not a typo —
it is the documentation of a skip.

**A fresh shape, 2026-08-15 — routing an obligation onto a surface a sibling doesn't serve.** When
routing `reflection_endpoints` (§4.5.1) to rust and py, the report instructed each to apply the
field on "both surfaces" because §2.2 binds the wrapped **and** the §9.2 unwrapped `advertise`.
**Both peers corrected it: neither serves the unwrapped surface** (rust prints `surface: wrapped`,
py has no `create_datagram_endpoint`), so §2.2's "both" collapses to the one wrapped surface in all
three — as it does here. I verified the *field* was absent but asserted the *surface existed*
without opening the tree — the "ahead of reality" defect in a new dress: a §2.2 obligation is only
routable onto a surface the target actually serves. **Before routing "MUST appear on both
surfaces," confirm the sibling serves the second surface** (`git grep` the listener, not just the
field). Same root cause as the flag-help staleness — a build-state claim asserted, not read.

**RATIFIED 2026-08-15 (f) — the sentence about the sibling is the one nobody verifies.** This has
now bitten **twice in two sessions**, both from a grep miss on a *sibling's* tree: the "both
surfaces" claim above, and a report that called py's §5.4.1 status **"unverified"** when py had
`test_keepalive_no_cadence_writes` passing all along — I searched `keepalive`+`write_status` and the
test's name did not match, so a present regression test read as an absence. Rust and py each
independently promoted this to a discipline in their own charters; it is earned here too (bit us
twice, different shapes → ratified, per the promotion ladder). **The rule: a claim about a sibling's
state — built / not-built / verified / unverified — gets the *same* open-the-tree standard as a
claim about our own, and a NEGATIVE about a sibling ("absent", "unverified", "built nowhere")
requires an exhaustive named search, including their TESTS, not a single grep of production code.**
A grep that finds nothing is not evidence of absence; it is evidence your pattern missed. The
enforcement point: before a report asserts a sibling lacks something, `git grep` the sibling for the
feature by **behavior and by test name**, not one keyword — the reports are right about their author
every time and wrong about the neighbor, because the neighbor sentence is the one that ships
unverified. (Corrected instances: `docs/validation/reports/2026-08-15-{b,e}-*.md`.)

**2026-08-16 — cite the commit that CONTAINS the work, not the commit that REPORTS it (arch's AP-12).**
We pinned py's v1.23 collector at `a6140a8` and stated it "built its collector." Arch, verifying rather
than accepting, found `a6140a8` is **docs-only** (an `AGENTS.md` + a status doc — and titled, aptly,
*"ratify the hearsay discipline — a report was wrong about us twice"*); the actual collector is
`5fbb8f3` (+233 / +370 tests). The conclusion was right, only the pin was wrong — but a pin is a
build-state claim, and a docs/status commit that *describes* a feature is not the feature. This is the
same family as the flag-help staleness and the sibling-negative miss: a sentence about a neighbor that
reads correct and wasn't opened. **The enforcement point: a commit pin asserting a sibling built X must
name a commit whose DIFF contains X (`git show --stat <pin>` shows the code, not just docs); a
docs/`AGENTS.md`/status-only commit is never the citation for an implementation.** Arch notes this class
has bitten four times and they've caught two — the peer verifying our pins is doing work we owe
ourselves.

**2026-08-19 — a subagent's claim about a sibling is STILL a build-state claim; pin it to the live
symbol at HEAD before routing it (earned — arch caught a three-way divergence I routed that had been
closed for a week).** The registry drip-feed packet claimed the §6a.9 layer-1 error strings diverged
three ways (`rust invalid_signature` / `py proof_failed` / `go signature_invalid`). I verified go's
own constant directly, but relayed rust's and py's from **sibling-inventory subagents that read a
COMMENT recording a *previous* value** and reported it as current — the strings had converged to
`signature_invalid` on 2026-08-12. The existing sibling-negative rule ("a claim about a neighbor gets
the same open-the-tree standard as a claim about us, and a negative needs an exhaustive named search
including tests") already covered *me* reading a sibling; the new clause is that it covers a
**delegated** read too. Enforcement: a subagent reporting "impl X answers Y / lacks Z" is reporting a
build-state claim, and "the subagent found the string in the tree" is NOT "the current constant is
that string" when the hit sits in a comment, a historical note, or a stale doc — **spot-verify a
cross-impl DIVERGENCE claim against the live symbol (`git grep` the constant at HEAD) before routing
it**, and when you delegate sibling recon, instruct the subagent to cite the defining symbol, not
prose. A divergence is the highest-stakes sibling claim there is (it routes a ruling), and it is the
one most worth one `git grep` before it ships.

**2026-08-16 — over-broad spec text reads as licensing (arch's AP-11), and OUR instance is the N1
listing, not the `is_error` grep.** Trace it (A1): Go implemented (A) — embed+materialize a compute/error
into a construct field — and the old `eval_construct.go` comment cites its source explicitly, *"§2.3 N1 +
§2.4."* N1 listed `compute/error` among the values "placed into … construct fields," and we read that
listing as licensing (A). That is the real bite, and it is what the v3.23 correction confirms: *"it is
where one implementation's reading of the construct branch came from."* (Arch's own example of the same
class — a reader grepping `is_error`, undefined in EXTENSION-COMPUTE, finding CONTINUATION's
`status >= 400` — is a PARALLEL instance, not ours; do not report it as though it bit Go.) The earned,
enforceable rule: **when a spec predicate or list we branch on is undefined or over-broad in its own
document, that is a spec-issue to ROUTE, not licensing to implement from** — and, having implemented it,
re-reading the fold with this lens is what surfaced the two residual mentions
(`docs/validation/spec-issues/2026-08-16-*`). Dead/over-broad spec text is not inert; it reads as
permission.

**A `[MUST]` that needs a value but names no config site — and no vector — is unimplementable-consistently:
route it, do NOT invent the site (earned 2026-08-19, four bites in one shape — the registry drip-feed audit).**
core-py named the class exactly: *"a `[MUST when present]` with no declared site is a rule two conformant
peers cannot both implement."* The proof is measured, not argued: EXTENSION-REGISTRY §6a.9.1 makes the
resolver-side TTL ceiling `min(binding.ttl, local_max)` a `[MUST when present]` and the spec itself calls
it *"the load-bearing one"* — but declares no `local_max` field, so **go put it in a builder option
(`WithLocalMaxTTL`), py in `resolver_chain[].hints.max_ttl`, rust in a third place: three impls "conformant,"
none interoperable, an operator's config artifact clamping on zero of them.** Same shape bit four times
(SA-PY-15, SA-PY-21 Q2 the MAY-override-with-no-field, R-OPEN-1 the register-request code strings, `2026-08-18-a`
the mint refusal code). This is the **spec-reading** sibling of the surface-enumeration family: those rules
guard *our own* impl surfaces; this one detects *under-specification in the spec we consume*, before code.
**Enforcement: on a new `[MUST]`/`[MUST when present]` that names a value, confirm the value has (a) a declared
config field and (b) a conformance vector; if either is absent, that is the spec-issue to route BEFORE
implementing — and inventing the site is the anti-pattern, because three invented sites is three homes no
operator can target.** The corollary the same audit earned: **a wildcard-MATCHING rule being unified (registry
v1.15, "the ONLY name matcher") does NOT unify character-LEGALITY** — §6.3 (no `/`) vs §11.1 vectors (`/`-names
matched) are separate regimes; "what `*` means" and "what bytes a name may contain" are two rules, and a
conformance vector that asserts one while violating the other is unconstructible on the wire (`2026-08-19-b`).
The audit + the consolidated cohort packet for arch: `docs/reviews/2026-08-19-registry-drip-feed-audit-and-consolidated-arch-packet.md`.
Its process finding is not a go discipline but belongs on the record: the drip-feed's root is **no single
reconciled open-items ledger** — "board closed" gets declared on one party's local view while items sit open
on another's (SA-PY-15 was dropped, R-OPEN-1 never entered arch's board), so gaps are rediscovered and routed
one tile at a time instead of ruled as a family.

> **The INVERSE of the above bit 2026-09-04 (SA-PY-40, corrected 2026-09-05): before routing "this slot has
> no governing code," read the status ROW itself for a named default — a row default governs the generic case
> whether or not an operation-specific table exists, and the absence of a table is NOT the absence of a code
> (candidate; go and py made the identical misread, arch caught it).** SA-PY-40 routed "the §3.3 400 slot has
> no enumerated set." Wrong: §3.3's 400 ROW (ENTITY-CORE-PROTOCOL line 830) had named `invalid_request` the
> default — "the generic 400 code an extension handler uses for a structurally invalid request" — since before
> the arc. The misread came from reading the *satisfaction-mode cross-reference* ("400 by the enumerated
> specific sets") and the delegation line in isolation, and fixating on "tree:put has no Appendix A row," while
> never reading the 400 row's own default. The real defect was one level subtler and arch named it: 0.8.2.7
> stated the permission (MAY use a specific) and the prohibition (undefined is non-conformant) but never the
> **consequence** — so an undefined spelling had no stated disposition, and two seats who read §3.3 hardest all
> week both concluded the slot was ungoverned. **When two careful readers reach the same wrong conclusion from
> a technically-correct sentence, the sentence is the defect** — the right move (which go+py did) was to ROUTE
> it, and 0.8.2.9 fixed the sentence (undefined → falls back to the row default; a needed specific is a named
> divergence to route, not invent — go's own discipline, now in the spec). Enforcement: a spec-issue asserting
> "no code / no set exists for X at status N" MUST first quote status N's ROW default (or state there is none);
> "operation X has no table" is a different, weaker claim and does not imply "N has no default." Re-application
> of D12 (read the canonical source — the row, not the cross-ref); recorded because the enforcement point
> (quote the row default before claiming absence) is concrete and it cost a misfiled spec-issue on two seats.
> **Companion, same cycle — verify a routing packet's per-SITE worklist against the spec ROWS and the code
> path, because one code-site can span two rows:** arch's worklist said "go's 2 tree:put sites → hash_mismatch,"
> but `entity.Validate()` checks empty-type/data BEFORE the hash, so the validate site spans the
> `invalid_request` (structural) AND `hash_mismatch` (content-hash) rows — flipping it wholesale mis-codes the
> structural case. The Appendix A ROWS were right (two distinct 400 rows); only the prose summary was
> imprecise. go branched on `errors.Is(err, hash.ErrHashMismatch)` and the empty-type arm is the mutation
> witness. Re-application of "trace the value / read the code, not the summary" (A1). Both landed at
> `<cycle-4 commit>`; report `docs/validation/reports/2026-09-05-a-*`, spec-issue `2026-09-04-a` (resolved).
>
> **Third bite of the same inverse, 2026-09-10 — before routing "the spec names no SITE for value X," open the
> spec that OWNS the noun X, not the one the question arrived phrased in (RATIFIED; arch declined go's spec-issue
> `2026-09-10-a`, and it was the third time in one arc the answer was already in the corpus).** go routed
> `2026-09-10-a` asking for a declared `peers` site for a subscription `deliver_token`, explicitly invoking the
> "a `[MUST]` that needs a value but names no config site — route it" shape on the peers axis. The site existed:
> **EXTENSION-SUBSCRIPTION §1.2 line 106 already declares the deliver_token is A-rooted** (rooted at the peer that
> owns the target inbox), so its chain-ROOT granter is the delivery target — which under E1 relaxes Dimension 4
> with no separate `peers` value. go framed a question about a **deliver_token's rooting** in §5.2/PD-2 (where the
> outbound check lives) instead of EXTENSION-SUBSCRIPTION §1.2 (which DEFINES the deliver_token), and routed an
> issue the owning spec had already answered — while go's own `CreateDeliveryTokenWithTTL` minted a **B-rooted**
> token (`Parent` = the connection grant), non-conformant against that same §1.2. Same failure as the F67 arc's
> token sweep and the two-level model (§6.8 owned F67, CONTINUATION §3.6b owned the levels, SUBSCRIPTION §1.2
> owns the deliver_token) — every one found by opening the spec that owns the noun, not the one the question was
> phrased in. **Enforcement (a sharpening of D12 + the SA-PY-40 inverse, promoted by the third bite): before you
> ROUTE a spec-issue about a value/behavior of a named artifact (a `deliver_token`, an error `code`, a `granter`),
> name the artifact's DEFINING spec and quote its rule; a spec-issue framed in the spec where the question arose,
> when the answer lives in the spec that owns the artifact, is a self-inflicted round trip — and it can co-exist
> with your own non-conformance against that owning spec (as go's B-rooted token did).** Owed: the A-rooted
> deliver_token fix + routing delivery through the gate (handoff); the interim at `6319e3e` is superseded.

> **A TWO-ARM §3.3 rule half-applied — path_required (absent) enforced, ambiguous_resource (more-than-one) NOT — is a landed rule shipped at 50%, and it is invisible to BOTH the wire and the citation register; the only finder is a tree census, and the trigger for the census is a cohort signal (candidate, 2026-09-10 — rust AND py each independently found and closed the identical class in their trees, which is the two-siblings-agree form of "the cross-impl FAIL is the true signal").** 0.8.2.18 split §3.3's 400 row into ONE two-input rule: an absent resource → `path_required`, more than one → `ambiguous_resource`, for any op that derives its target from `Resource.Targets[0]`. go implemented BOTH arms at register/unregister (`ext/handlers`), compute (handler+engine), and continuation/install — but `ext/content`'s `requireResource` (the gate for `system/content:{get,ingest}`) enforced only the absent arm and **silently acted on `Targets[0]` when handed several** — exactly py's finding (its census gate reddened `system/content:{get,ingest}`, "not in my predicted set") and rust's (ROLE's shared helper wrong in both directions). Why nothing here caught it: a probe cannot — no client sends two targets by accident (py's words); the conformance register cannot — the gap is behavioral, not a citation, so pass 0b is blind; and go-on-go is green because go's own callers send one target. **Only reading the tree finds it, and what tells you to read the tree is two independent siblings reporting the same class.** Enforcement, grep-able: for every `Resource.Targets[0]` read that enforces the absent arm (`len(Targets)==0 → path_required`), confirm the SAME site also guards `len(Targets) > 1 → ambiguous_resource` — a site with one arm and not the other is a half-applied 0.8.2.18. `git grep 'Targets\[0\]'` across `core/ ext/` and diff each site's guard against the register/compute/continuation pattern; a site missing the `> 1` arm is the defect whether or not any run is red. The finder in every case was a sibling report, not a red run — the two-siblings-agree form of "the cross-impl signal is the true signal." **BUT the fix I first landed (a raw-`len(Targets) > 1` guard at content, `39c3650`) was the WRONG ABSTRACTION and I REVERTED it — which is the sharper, second lesson (candidate → this is the bite that promotes it): closing the NAMED arm is not the same as counting the RIGHT SET, and a guard that looks complete can be superseded by a ruling that redefines what is counted.** Answering go's own "one target + a non-empty exclude is a third shape" question, arch (F68, `ROUTING-2026-09-10-d` §2) found a live cross-impl BYPASS and ruled the §3.3 count is taken on the **EFFECTIVE target set = `targets` minus the caller's own `exclude`** (the set §5.2 already names), not on raw `targets`: 0 effective → `path_required` (covers a lone self-excluded target), >1 → `ambiguous_resource`, 1 → proceed; and the transferable rule **a handler MUST NOT act on a target the authorization check SKIPPED** (a gate that narrows an input and a consumer that re-widens it is a bypass however correct each half is alone — F67's shape). go is unaffected by the bypass ONLY because go never implemented caller `Resource.Exclude` on the authorization path (`CheckResourceScope` authorizes every raw target; stricter than §5.2, its own divergence) — but that same fact means a raw-`len(Targets)` guard is exactly the "correction in one home the next revision moves" arch named as F68's shape. So content is REVERTED to the absent arm only and folds into the single `0.8.2.20` effective-target sweep alongside `DOMAIN-LOCAL-FILES` (§4.1: `targets[0]` with no guard at all) and the five §2.8 sites — one revision, not five partial corrections. **Enforcement (the promoted rule): when a landed two-input rule is about to be re-based onto a different COUNTED SET by an imminent ruling (here raw→effective targets), do NOT land a per-site guard on the old set — it is superseded within the week and reads "done" while the real sweep is owed; wait for the revision and sweep once on the new set.** The census enforcement still stands for FINDING the gap (`git grep 'Targets\[0\]'`, diff each site's guard), but the FIX is a check-set requirement (a two-target vector + a self-excluded-target security vector per op, `L17`'s missing half) folded into `0.8.2.20`, not a raw guard. Recorded: the first bite (cohort-wide half-rule, found by census) AND the second (the obvious fix was the wrong set, caught by arch's F68 ruling before it calcified). The other unguarded `Targets[0]` sites (tree, identity, localfiles, subscription) are the same sweep — NOT converged here.

**A newly-landed spec clause that CONTRADICTS a prior landed ruling in the same corpus is a spec-issue to
ROUTE, not intent to implement — and the tell you got it wrong is that you can NAME the contradiction
(earned 2026-08-20, arch caught it; the v3.24 `assoc` corner).** EXTENSION-COMPUTE v3.24 §3.5 told go to answer an
out-of-range `assoc` index with `type_mismatch` — the exact opposite of §2.2's landed cross-impl ruling (F-2,
*"an out-of-bounds magnitude is not a type error"* → `index_out_of_range`), a ruling go had itself been corrected
on one operation earlier. go implemented the contradiction faithfully **and wrote a test literally named
`out-of-range-is-type-mismatch-NOT-index-out-of-range`** — the name is the confession: I noticed the departure
from a prior ruling and coded it as if the departure were the intent. It also propagated (`range` anchored its
`type_mismatch` to the `assoc` defect), so one mis-read reached two primitives. This is the CONTRADICTION sibling
of the over-broad/dead-spec-text rule ("dead spec text reads as permission," 2026-08-16) — that one is
*under*-specification read as licensing; this one is a *definite-but-wrong* clause read as a deliberate exception.
Neither is licensing. **Enforcement, sharp: if you can write a test whose NAME asserts `X-NOT-Y` where `Y` was a
prior ruling — or otherwise articulate "this clause departs from the earlier decision on the same question" — that
articulation is the STOP: a clause contradicting a landed ruling is routed as a spec-issue (with the prior ruling
cited), and the unambiguous rest is implemented, but the contradicted clause is NOT shipped as intent.** A spec is
one document; it must not answer one malformed program two ways depending on which operation reached it, and when it
does, the impl that notices owes the route, not the faithful reproduction. (Fixed: `assoc` → `index_out_of_range`,
`range` → the new `count_out_of_range`; `group-by` re-pinned to `system/compute/group{key,members}` — that corner
was arch's fold losing a ruling, not go's contradiction, so it earns no go discipline.)

**When a routing packet says "do not implement from this prose — the proposal is the text" AND flags a
disposition as owed to a future spec fold, a two-reading disposition WORD is a STOP, not a choice: implement
only what arch explicitly UNGATED and defer the ambiguous disposition to the folded text — do not pick a
reading (candidate, first bite 2026-08-21, rust + py both caught it; the §3.5 Corner 1 fold).** Arch's C-8
routing (`ROUTING-2026-08-21-e` §6) said verbatim *"do not implement from this packet's prose … D2 rewrites a
sentence you have already built against,"* and §2.1's fold row read `"contain / propagate"` with the bullet
*"fold's accumulator propagates"* — where "propagate" means *threads onward into the next closure call*, i.e.
CONTAIN. go read it as *short-circuit* and shipped `builtinFold` aborting on an error accumulator (9ad0110) —
the exact opposite of the ruling, on the one position arch had flagged as widest-blast-radius *because the wrong
reading produces a different VALUE*. What arch had actually ungated was narrower and explicit: only the
map/provenance §2.4 restatement (*"the fix is not gated on the proposal landing"*); the fold disposition was
owed to v3.27 and go implemented it anyway from a summary word. This is the ACTIONABLE half of py's *"a one-word
disposition column is where a two-reading word loses its second reading"* — that names the failure; this names
the trigger and the stop. It is a sibling of the CONTRADICTION rule above (both are misreadings of arch's routing
text), but distinct: that one is a *definite-but-wrong* clause read as intent; this one is an *ambiguous* word in
a summary the packet told you not to implement from. **Enforcement: when a routing/summary defers to the proposal
text, grep the disposition you are about to implement for a two-reading verb (`propagate`/`contain`/`short-circuit`
used loosely), and if arch flagged the position as owed-to-the-fold, implement ONLY the explicitly-ungated part and
route/defer the rest — a disposition you can restate two ways is one you have not been given yet.** The
enforcement point that would have caught it: go-on-go could not — the 358 corpus lacked `CV-8d`, the discriminator
(this half re-applied, earns nothing new: *"a LOCK proves agreement only on inputs the corpus contains,"* the
CV-7a/b/c rule, biting again on a new vector; the corpus now carries CV-8d at SHA `1844d231`). The cross-impl
drive was the signal, as always. (Fixed: `d5e318d`; fold contains/recovers, `containOrPropagate` shared by
map/fold, both short-circuit sites mutation-verified.)

## Cross-impl validation deliverable

**A survey classification of "routed divergence, NOT a defect" requires a CONCLUSIVE control — an inconclusive control is a reason to INVESTIGATE, not a licence to call the other seat's result conformant (candidate, first bite 2026-09-13, py caught it in go's own survey).** go's three-way survey filed py's `include_payload_overlapping_exclude` 403/403 as "already-routed Q2 divergence, not a defect" — every clause true, conclusion wrong. It was an ordinary py bug (a wrong handler argument), and **a question being open upstream (routed to arch) does not make the behaviour under it conformant.** The survey itself noted py failed the well-formed positive control (denied the non-overlapping subject too); a peer that fails the control cannot attribute its refusal to the thing under test. **Enforcement: before classifying a cross-impl result "conformant divergence, routed," confirm the peer PASSES the arm's positive control; if it fails the control, the classification is "likely defect — investigate," and routing the spec question is a SEPARATE act from blessing the behaviour.** The divergence rule governs whether *go* changes, never whether the *other seat's* result is a bug — conflating the two mislabels a defect as a spec question and stops the investigation that would have caught it.

When validating Python/Rust against a spec change, the deliverable is a **written per-peer report under `docs/validation/reports/`** (dated filename, matching prior reports: headline pass/partial/blocked, category table, each gap with spec requirement + probed request shape + peer response + why it matters + suggested fix/test, repro steps) — not chat output that evaporates. Run the validator, report what you observed, and stop: don't dig through the sibling's git history or internals to root-cause — that's a separate job.
